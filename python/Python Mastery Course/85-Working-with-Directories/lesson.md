# Working with Directories

## Overview
Create, list, traverse, and manage directories in Python. Build file explorers, directory trees, and batch file processors.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply os.listdir() and os.scandir()
2. Understand and apply pathlib.iterdir() and glob()
3. Understand and apply creating directories (mkdir, makedirs)
4. Understand and apply directory tree traversal (os.walk)
5. Understand and apply filtering files by extension
6. Understand and apply directory copying and moving
7. Understand and apply directory size calculation
8. Understand and apply temporary directories (tempfile.temporarydirectory)
9. Debug common errors and edge cases related to working with directories
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **os.listdir() and os.scandir()** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **pathlib.iterdir() and glob()** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Creating directories (mkdir, makedirs)** | Enables practical implementation | Everyday Python development |
| 4 | **Directory tree traversal (os.walk)** | Used in real-world applications | Advanced scenarios |
| 5 | **Filtering files by extension** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Directory copying and moving** | Optimization and best practices | Advanced scenarios |
| 7 | **Directory size calculation** | Edge cases and error handling | Everyday Python development |
| 8 | **Temporary directories (tempfile.TemporaryDirectory)** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. os.listdir() and os.scandir()

os.listdir() and os.scandir() is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, os.listdir() and os.scandir() follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- os.listdir() and os.scandir() is essential for understanding working with directories thoroughly
- Mastery of os.listdir() and os.scandir() enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- os.listdir() and os.scandir() integrates with other Python features to create powerful, composable solutions

```python
# Example: os.listdir() and os.scandir()
# This demonstrates the core mechanics of os.listdir() and os.scandir()

def demonstrate_os_listdir_and_os_scandir():
    """Demonstrate os.listdir() and os.scandir() with a practical example."""
    # Setup and initialization
    result = None
    print("Running os.listdir() and os.scandir() demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_os_listdir_and_os_scandir()
```

### 2. pathlib.iterdir() and glob()

pathlib.iterdir() and glob() is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, pathlib.iterdir() and glob() follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- pathlib.iterdir() and glob() is essential for understanding working with directories thoroughly
- Mastery of pathlib.iterdir() and glob() enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- pathlib.iterdir() and glob() integrates with other Python features to create powerful, composable solutions

```python
# Example: pathlib.iterdir() and glob()
# This demonstrates the core mechanics of pathlib.iterdir() and glob()

def demonstrate_pathlib_iterdir_and_glob():
    """Demonstrate pathlib.iterdir() and glob() with a practical example."""
    # Setup and initialization
    result = None
    print("Running pathlib.iterdir() and glob() demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_pathlib_iterdir_and_glob()
```

### 3. Creating directories (mkdir, makedirs)

Creating directories (mkdir, makedirs) is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, creating directories (mkdir, makedirs) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Creating directories (mkdir, makedirs) is essential for understanding working with directories thoroughly
- Mastery of creating directories (mkdir, makedirs) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Creating directories (mkdir, makedirs) integrates with other Python features to create powerful, composable solutions

```python
# Example: Creating directories (mkdir, makedirs)
# This demonstrates the core mechanics of creating directories (mkdir, makedirs)

def demonstrate_creating_directories_mkdir_makedirs():
    """Demonstrate Creating directories (mkdir, makedirs) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Creating directories (mkdir, makedirs) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_creating_directories_mkdir_makedirs()
```

### 4. Directory tree traversal (os.walk)

Directory tree traversal (os.walk) is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, directory tree traversal (os.walk) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Directory tree traversal (os.walk) is essential for understanding working with directories thoroughly
- Mastery of directory tree traversal (os.walk) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Directory tree traversal (os.walk) integrates with other Python features to create powerful, composable solutions

```python
# Example: Directory tree traversal (os.walk)
# This demonstrates the core mechanics of directory tree traversal (os.walk)

def demonstrate_directory_tree_traversal_os_walk():
    """Demonstrate Directory tree traversal (os.walk) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Directory tree traversal (os.walk) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_directory_tree_traversal_os_walk()
```

### 5. Filtering files by extension

Filtering files by extension is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, filtering files by extension follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Filtering files by extension is essential for understanding working with directories thoroughly
- Mastery of filtering files by extension enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Filtering files by extension integrates with other Python features to create powerful, composable solutions

```python
# Example: Filtering files by extension
# This demonstrates the core mechanics of filtering files by extension

def demonstrate_filtering_files_by_extension():
    """Demonstrate Filtering files by extension with a practical example."""
    # Setup and initialization
    result = None
    print("Running Filtering files by extension demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_filtering_files_by_extension()
```

### 6. Directory copying and moving

Directory copying and moving is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, directory copying and moving follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Directory copying and moving is essential for understanding working with directories thoroughly
- Mastery of directory copying and moving enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Directory copying and moving integrates with other Python features to create powerful, composable solutions

```python
# Example: Directory copying and moving
# This demonstrates the core mechanics of directory copying and moving

def demonstrate_directory_copying_and_moving():
    """Demonstrate Directory copying and moving with a practical example."""
    # Setup and initialization
    result = None
    print("Running Directory copying and moving demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_directory_copying_and_moving()
```

### 7. Directory size calculation

Directory size calculation is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, directory size calculation follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Directory size calculation is essential for understanding working with directories thoroughly
- Mastery of directory size calculation enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Directory size calculation integrates with other Python features to create powerful, composable solutions

```python
# Example: Directory size calculation
# This demonstrates the core mechanics of directory size calculation

def demonstrate_directory_size_calculation():
    """Demonstrate Directory size calculation with a practical example."""
    # Setup and initialization
    result = None
    print("Running Directory size calculation demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_directory_size_calculation()
```

### 8. Temporary directories (tempfile.TemporaryDirectory)

Temporary directories (tempfile.TemporaryDirectory) is a fundamental concept in Python working with directories. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, temporary directories (tempfile.temporarydirectory) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Temporary directories (tempfile.TemporaryDirectory) is essential for understanding working with directories thoroughly
- Mastery of temporary directories (tempfile.temporarydirectory) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Temporary directories (tempfile.TemporaryDirectory) integrates with other Python features to create powerful, composable solutions

```python
# Example: Temporary directories (tempfile.TemporaryDirectory)
# This demonstrates the core mechanics of temporary directories (tempfile.temporarydirectory)

def demonstrate_temporary_directories_tempfile_temporarydirectory():
    """Demonstrate Temporary directories (tempfile.TemporaryDirectory) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Temporary directories (tempfile.TemporaryDirectory) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_temporary_directories_tempfile_temporarydirectory()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│               Working with Directories                │
├───────────────────────────────────────────────────────┤
│             os.listdir() and os.scandir()             │
│                           │                           │
│                           ▼                           │
│             pathlib.iterdir() and glob()              │
│                           │                           │
│                           ▼                           │
│        Creating directories (mkdir, makedirs)         │
│                           │                           │
│                           ▼                           │
│          Directory tree traversal (os.walk)           │
│                           │                           │
│                           ▼                           │
│             Filtering files by extension              │
│                           │                           │
│                           ▼                           │
│             Directory copying and moving              │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with working with directories
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating working with directories
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using os.listdir() and os.scandir()
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating os.listdir() and os.scandir()."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating working with directories concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- os.listdir() and os.scandir() is the foundation of working with directories in Python
- pathlib.iterdir() and glob() builds on this foundation for more advanced use cases
- Creating directories (mkdir, makedirs) enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding working with directories deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Working with Directories in Python. You learned about os.listdir() and os.scandir(), pathlib.iterdir() and glob(), Creating directories (mkdir, makedirs), Directory tree traversal (os.walk), and
practical techniques for Filtering files by extension, Directory copying and moving. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
