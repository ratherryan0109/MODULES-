# Selection Sort

## Overview
Implement selection sort — O(n²) sort with minimal swaps. Understand selection vs bubble vs insertion tradeoffs.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply selection sort algorithm
2. Understand and apply finding minimum in unsorted portion
3. Understand and apply swapping minimum to position
4. Understand and apply o(n²) always (no early exit)
5. Understand and apply o(n) swaps (good for expensive writes)
6. Understand and apply selection sort stability
7. Understand and apply selection sort vs bubble sort
8. Understand and apply when selection sort is useful
9. Debug common errors and edge cases related to selection sort
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **Selection sort algorithm** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **Finding minimum in unsorted portion** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Swapping minimum to position** | Enables practical implementation | Everyday Python development |
| 4 | **O(n²) always (no early exit)** | Used in real-world applications | Advanced scenarios |
| 5 | **O(n) swaps (good for expensive writes)** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Selection sort stability** | Optimization and best practices | Advanced scenarios |
| 7 | **Selection sort vs bubble sort** | Edge cases and error handling | Everyday Python development |
| 8 | **When selection sort is useful** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. Selection sort algorithm

Selection sort algorithm is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, selection sort algorithm follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Selection sort algorithm is essential for understanding selection sort thoroughly
- Mastery of selection sort algorithm enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Selection sort algorithm integrates with other Python features to create powerful, composable solutions

```python
# Example: Selection sort algorithm
# This demonstrates the core mechanics of selection sort algorithm

def demonstrate_selection_sort_algorithm():
    """Demonstrate Selection sort algorithm with a practical example."""
    # Setup and initialization
    result = None
    print("Running Selection sort algorithm demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_selection_sort_algorithm()
```

### 2. Finding minimum in unsorted portion

Finding minimum in unsorted portion is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, finding minimum in unsorted portion follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Finding minimum in unsorted portion is essential for understanding selection sort thoroughly
- Mastery of finding minimum in unsorted portion enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Finding minimum in unsorted portion integrates with other Python features to create powerful, composable solutions

```python
# Example: Finding minimum in unsorted portion
# This demonstrates the core mechanics of finding minimum in unsorted portion

def demonstrate_finding_minimum_in_unsorted_portion():
    """Demonstrate Finding minimum in unsorted portion with a practical example."""
    # Setup and initialization
    result = None
    print("Running Finding minimum in unsorted portion demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_finding_minimum_in_unsorted_portion()
```

### 3. Swapping minimum to position

Swapping minimum to position is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, swapping minimum to position follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Swapping minimum to position is essential for understanding selection sort thoroughly
- Mastery of swapping minimum to position enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Swapping minimum to position integrates with other Python features to create powerful, composable solutions

```python
# Example: Swapping minimum to position
# This demonstrates the core mechanics of swapping minimum to position

def demonstrate_swapping_minimum_to_position():
    """Demonstrate Swapping minimum to position with a practical example."""
    # Setup and initialization
    result = None
    print("Running Swapping minimum to position demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_swapping_minimum_to_position()
```

### 4. O(n²) always (no early exit)

O(n²) always (no early exit) is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, o(n²) always (no early exit) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- O(n²) always (no early exit) is essential for understanding selection sort thoroughly
- Mastery of o(n²) always (no early exit) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- O(n²) always (no early exit) integrates with other Python features to create powerful, composable solutions

```python
# Example: O(n²) always (no early exit)
# This demonstrates the core mechanics of o(n²) always (no early exit)

def demonstrate_o_n_always_no_early_exit():
    """Demonstrate O(n²) always (no early exit) with a practical example."""
    # Setup and initialization
    result = None
    print("Running O(n²) always (no early exit) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_o_n_always_no_early_exit()
```

### 5. O(n) swaps (good for expensive writes)

O(n) swaps (good for expensive writes) is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, o(n) swaps (good for expensive writes) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- O(n) swaps (good for expensive writes) is essential for understanding selection sort thoroughly
- Mastery of o(n) swaps (good for expensive writes) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- O(n) swaps (good for expensive writes) integrates with other Python features to create powerful, composable solutions

```python
# Example: O(n) swaps (good for expensive writes)
# This demonstrates the core mechanics of o(n) swaps (good for expensive writes)

def demonstrate_o_n_swaps_good_for_expensive_writes():
    """Demonstrate O(n) swaps (good for expensive writes) with a practical example."""
    # Setup and initialization
    result = None
    print("Running O(n) swaps (good for expensive writes) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_o_n_swaps_good_for_expensive_writes()
```

### 6. Selection sort stability

Selection sort stability is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, selection sort stability follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Selection sort stability is essential for understanding selection sort thoroughly
- Mastery of selection sort stability enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Selection sort stability integrates with other Python features to create powerful, composable solutions

```python
# Example: Selection sort stability
# This demonstrates the core mechanics of selection sort stability

def demonstrate_selection_sort_stability():
    """Demonstrate Selection sort stability with a practical example."""
    # Setup and initialization
    result = None
    print("Running Selection sort stability demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_selection_sort_stability()
```

### 7. Selection sort vs bubble sort

Selection sort vs bubble sort is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, selection sort vs bubble sort follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Selection sort vs bubble sort is essential for understanding selection sort thoroughly
- Mastery of selection sort vs bubble sort enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Selection sort vs bubble sort integrates with other Python features to create powerful, composable solutions

```python
# Example: Selection sort vs bubble sort
# This demonstrates the core mechanics of selection sort vs bubble sort

def demonstrate_selection_sort_vs_bubble_sort():
    """Demonstrate Selection sort vs bubble sort with a practical example."""
    # Setup and initialization
    result = None
    print("Running Selection sort vs bubble sort demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_selection_sort_vs_bubble_sort()
```

### 8. When selection sort is useful

When selection sort is useful is a fundamental concept in Python selection sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, when selection sort is useful follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- When selection sort is useful is essential for understanding selection sort thoroughly
- Mastery of when selection sort is useful enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- When selection sort is useful integrates with other Python features to create powerful, composable solutions

```python
# Example: When selection sort is useful
# This demonstrates the core mechanics of when selection sort is useful

def demonstrate_when_selection_sort_is_useful():
    """Demonstrate When selection sort is useful with a practical example."""
    # Setup and initialization
    result = None
    print("Running When selection sort is useful demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_when_selection_sort_is_useful()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                    Selection Sort                     │
├───────────────────────────────────────────────────────┤
│               Selection sort algorithm                │
│                           │                           │
│                           ▼                           │
│          Finding minimum in unsorted portion          │
│                           │                           │
│                           ▼                           │
│             Swapping minimum to position              │
│                           │                           │
│                           ▼                           │
│             O(n²) always (no early exit)              │
│                           │                           │
│                           ▼                           │
│        O(n) swaps (good for expensive writes)         │
│                           │                           │
│                           ▼                           │
│               Selection sort stability                │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with selection sort
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating selection sort
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using Selection sort algorithm
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating Selection sort algorithm."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating selection sort concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- Selection sort algorithm is the foundation of selection sort in Python
- Finding minimum in unsorted portion builds on this foundation for more advanced use cases
- Swapping minimum to position enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding selection sort deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Selection Sort in Python. You learned about Selection sort algorithm, Finding minimum in unsorted portion, Swapping minimum to position, O(n²) always (no early exit), and
practical techniques for O(n) swaps (good for expensive writes), Selection sort stability. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
