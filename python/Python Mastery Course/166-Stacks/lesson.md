# Stacks

## Overview
Implement stacks in Python — LIFO data structure. Understand push, pop, peek operations and real-world applications.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply stack lifo principle
2. Understand and apply stack using list (append, pop)
3. Understand and apply stack using collections.deque
4. Understand and apply stack operations (push, pop, peek, is_empty)
5. Understand and apply undo/redo with stacks
6. Understand and apply expression parsing (parentheses match)
7. Understand and apply dfs with stack
8. Understand and apply call stack recursion insight
9. Debug common errors and edge cases related to stacks
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **Stack LIFO principle** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **Stack using list (append, pop)** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Stack using collections.deque** | Enables practical implementation | Everyday Python development |
| 4 | **Stack operations (push, pop, peek, is_empty)** | Used in real-world applications | Advanced scenarios |
| 5 | **Undo/redo with stacks** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Expression parsing (parentheses match)** | Optimization and best practices | Advanced scenarios |
| 7 | **DFS with stack** | Edge cases and error handling | Everyday Python development |
| 8 | **Call stack recursion insight** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. Stack LIFO principle

Stack LIFO principle is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, stack lifo principle follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Stack LIFO principle is essential for understanding stacks thoroughly
- Mastery of stack lifo principle enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Stack LIFO principle integrates with other Python features to create powerful, composable solutions

```python
# Example: Stack LIFO principle
# This demonstrates the core mechanics of stack lifo principle

def demonstrate_stack_lifo_principle():
    """Demonstrate Stack LIFO principle with a practical example."""
    # Setup and initialization
    result = None
    print("Running Stack LIFO principle demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_stack_lifo_principle()
```

### 2. Stack using list (append, pop)

Stack using list (append, pop) is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, stack using list (append, pop) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Stack using list (append, pop) is essential for understanding stacks thoroughly
- Mastery of stack using list (append, pop) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Stack using list (append, pop) integrates with other Python features to create powerful, composable solutions

```python
# Example: Stack using list (append, pop)
# This demonstrates the core mechanics of stack using list (append, pop)

def demonstrate_stack_using_list_append_pop():
    """Demonstrate Stack using list (append, pop) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Stack using list (append, pop) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_stack_using_list_append_pop()
```

### 3. Stack using collections.deque

Stack using collections.deque is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, stack using collections.deque follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Stack using collections.deque is essential for understanding stacks thoroughly
- Mastery of stack using collections.deque enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Stack using collections.deque integrates with other Python features to create powerful, composable solutions

```python
# Example: Stack using collections.deque
# This demonstrates the core mechanics of stack using collections.deque

def demonstrate_stack_using_collections_deque():
    """Demonstrate Stack using collections.deque with a practical example."""
    # Setup and initialization
    result = None
    print("Running Stack using collections.deque demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_stack_using_collections_deque()
```

### 4. Stack operations (push, pop, peek, is_empty)

Stack operations (push, pop, peek, is_empty) is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, stack operations (push, pop, peek, is_empty) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Stack operations (push, pop, peek, is_empty) is essential for understanding stacks thoroughly
- Mastery of stack operations (push, pop, peek, is_empty) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Stack operations (push, pop, peek, is_empty) integrates with other Python features to create powerful, composable solutions

```python
# Example: Stack operations (push, pop, peek, is_empty)
# This demonstrates the core mechanics of stack operations (push, pop, peek, is_empty)

def demonstrate_stack_operations_push_pop_peek_is_empty():
    """Demonstrate Stack operations (push, pop, peek, is_empty) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Stack operations (push, pop, peek, is_empty) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_stack_operations_push_pop_peek_is_empty()
```

### 5. Undo/redo with stacks

Undo/redo with stacks is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, undo/redo with stacks follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Undo/redo with stacks is essential for understanding stacks thoroughly
- Mastery of undo/redo with stacks enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Undo/redo with stacks integrates with other Python features to create powerful, composable solutions

```python
# Example: Undo/redo with stacks
# This demonstrates the core mechanics of undo/redo with stacks

def demonstrate_undo_redo_with_stacks():
    """Demonstrate Undo/redo with stacks with a practical example."""
    # Setup and initialization
    result = None
    print("Running Undo/redo with stacks demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_undo_redo_with_stacks()
```

### 6. Expression parsing (parentheses match)

Expression parsing (parentheses match) is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, expression parsing (parentheses match) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Expression parsing (parentheses match) is essential for understanding stacks thoroughly
- Mastery of expression parsing (parentheses match) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Expression parsing (parentheses match) integrates with other Python features to create powerful, composable solutions

```python
# Example: Expression parsing (parentheses match)
# This demonstrates the core mechanics of expression parsing (parentheses match)

def demonstrate_expression_parsing_parentheses_match():
    """Demonstrate Expression parsing (parentheses match) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Expression parsing (parentheses match) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_expression_parsing_parentheses_match()
```

### 7. DFS with stack

DFS with stack is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, dfs with stack follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- DFS with stack is essential for understanding stacks thoroughly
- Mastery of dfs with stack enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- DFS with stack integrates with other Python features to create powerful, composable solutions

```python
# Example: DFS with stack
# This demonstrates the core mechanics of dfs with stack

def demonstrate_dfs_with_stack():
    """Demonstrate DFS with stack with a practical example."""
    # Setup and initialization
    result = None
    print("Running DFS with stack demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_dfs_with_stack()
```

### 8. Call stack recursion insight

Call stack recursion insight is a fundamental concept in Python stacks. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, call stack recursion insight follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Call stack recursion insight is essential for understanding stacks thoroughly
- Mastery of call stack recursion insight enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Call stack recursion insight integrates with other Python features to create powerful, composable solutions

```python
# Example: Call stack recursion insight
# This demonstrates the core mechanics of call stack recursion insight

def demonstrate_call_stack_recursion_insight():
    """Demonstrate Call stack recursion insight with a practical example."""
    # Setup and initialization
    result = None
    print("Running Call stack recursion insight demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_call_stack_recursion_insight()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                        Stacks                         │
├───────────────────────────────────────────────────────┤
│                 Stack LIFO principle                  │
│                           │                           │
│                           ▼                           │
│            Stack using list (append, pop)             │
│                           │                           │
│                           ▼                           │
│             Stack using collections.deque             │
│                           │                           │
│                           ▼                           │
│     Stack operations (push, pop, peek, is_empty)      │
│                           │                           │
│                           ▼                           │
│                 Undo/redo with stacks                 │
│                           │                           │
│                           ▼                           │
│        Expression parsing (parentheses match)         │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with stacks
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating stacks
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using Stack LIFO principle
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating Stack LIFO principle."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating stacks concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- Stack LIFO principle is the foundation of stacks in Python
- Stack using list (append, pop) builds on this foundation for more advanced use cases
- Stack using collections.deque enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding stacks deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Stacks in Python. You learned about Stack LIFO principle, Stack using list (append, pop), Stack using collections.deque, Stack operations (push, pop, peek, is_empty), and
practical techniques for Undo/redo with stacks, Expression parsing (parentheses match). With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
