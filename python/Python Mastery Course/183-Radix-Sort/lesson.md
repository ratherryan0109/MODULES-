# Radix Sort

## Overview
Implement radix sort — linear-time sorting for integers and strings. Understand digit-by-digit sorting and base choices.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply radix sort algorithm
2. Understand and apply lsd vs msd radix sort
3. Understand and apply digit extraction (by base)
4. Understand and apply stable digit-based sorting
5. Understand and apply o(d × (n + k)) complexity
6. Understand and apply base choice and performance
7. Understand and apply radix sort for strings
8. Understand and apply radix sort vs counting sort
9. Debug common errors and edge cases related to radix sort
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **Radix sort algorithm** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **LSD vs MSD radix sort** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Digit extraction (by base)** | Enables practical implementation | Everyday Python development |
| 4 | **Stable digit-based sorting** | Used in real-world applications | Advanced scenarios |
| 5 | **O(d × (n + k)) complexity** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Base choice and performance** | Optimization and best practices | Advanced scenarios |
| 7 | **Radix sort for strings** | Edge cases and error handling | Everyday Python development |
| 8 | **Radix sort vs counting sort** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. Radix sort algorithm

Radix sort algorithm is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, radix sort algorithm follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Radix sort algorithm is essential for understanding radix sort thoroughly
- Mastery of radix sort algorithm enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Radix sort algorithm integrates with other Python features to create powerful, composable solutions

```python
# Example: Radix sort algorithm
# This demonstrates the core mechanics of radix sort algorithm

def demonstrate_radix_sort_algorithm():
    """Demonstrate Radix sort algorithm with a practical example."""
    # Setup and initialization
    result = None
    print("Running Radix sort algorithm demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_radix_sort_algorithm()
```

### 2. LSD vs MSD radix sort

LSD vs MSD radix sort is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, lsd vs msd radix sort follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- LSD vs MSD radix sort is essential for understanding radix sort thoroughly
- Mastery of lsd vs msd radix sort enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- LSD vs MSD radix sort integrates with other Python features to create powerful, composable solutions

```python
# Example: LSD vs MSD radix sort
# This demonstrates the core mechanics of lsd vs msd radix sort

def demonstrate_lsd_vs_msd_radix_sort():
    """Demonstrate LSD vs MSD radix sort with a practical example."""
    # Setup and initialization
    result = None
    print("Running LSD vs MSD radix sort demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_lsd_vs_msd_radix_sort()
```

### 3. Digit extraction (by base)

Digit extraction (by base) is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, digit extraction (by base) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Digit extraction (by base) is essential for understanding radix sort thoroughly
- Mastery of digit extraction (by base) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Digit extraction (by base) integrates with other Python features to create powerful, composable solutions

```python
# Example: Digit extraction (by base)
# This demonstrates the core mechanics of digit extraction (by base)

def demonstrate_digit_extraction_by_base():
    """Demonstrate Digit extraction (by base) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Digit extraction (by base) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_digit_extraction_by_base()
```

### 4. Stable digit-based sorting

Stable digit-based sorting is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, stable digit-based sorting follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Stable digit-based sorting is essential for understanding radix sort thoroughly
- Mastery of stable digit-based sorting enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Stable digit-based sorting integrates with other Python features to create powerful, composable solutions

```python
# Example: Stable digit-based sorting
# This demonstrates the core mechanics of stable digit-based sorting

def demonstrate_stable_digit_based_sorting():
    """Demonstrate Stable digit-based sorting with a practical example."""
    # Setup and initialization
    result = None
    print("Running Stable digit-based sorting demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_stable_digit_based_sorting()
```

### 5. O(d × (n + k)) complexity

O(d × (n + k)) complexity is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, o(d × (n + k)) complexity follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- O(d × (n + k)) complexity is essential for understanding radix sort thoroughly
- Mastery of o(d × (n + k)) complexity enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- O(d × (n + k)) complexity integrates with other Python features to create powerful, composable solutions

```python
# Example: O(d × (n + k)) complexity
# This demonstrates the core mechanics of o(d × (n + k)) complexity

def demonstrate_o_d_n_k_complexity():
    """Demonstrate O(d × (n + k)) complexity with a practical example."""
    # Setup and initialization
    result = None
    print("Running O(d × (n + k)) complexity demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_o_d_n_k_complexity()
```

### 6. Base choice and performance

Base choice and performance is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, base choice and performance follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Base choice and performance is essential for understanding radix sort thoroughly
- Mastery of base choice and performance enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Base choice and performance integrates with other Python features to create powerful, composable solutions

```python
# Example: Base choice and performance
# This demonstrates the core mechanics of base choice and performance

def demonstrate_base_choice_and_performance():
    """Demonstrate Base choice and performance with a practical example."""
    # Setup and initialization
    result = None
    print("Running Base choice and performance demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_base_choice_and_performance()
```

### 7. Radix sort for strings

Radix sort for strings is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, radix sort for strings follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Radix sort for strings is essential for understanding radix sort thoroughly
- Mastery of radix sort for strings enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Radix sort for strings integrates with other Python features to create powerful, composable solutions

```python
# Example: Radix sort for strings
# This demonstrates the core mechanics of radix sort for strings

def demonstrate_radix_sort_for_strings():
    """Demonstrate Radix sort for strings with a practical example."""
    # Setup and initialization
    result = None
    print("Running Radix sort for strings demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_radix_sort_for_strings()
```

### 8. Radix sort vs counting sort

Radix sort vs counting sort is a fundamental concept in Python radix sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, radix sort vs counting sort follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Radix sort vs counting sort is essential for understanding radix sort thoroughly
- Mastery of radix sort vs counting sort enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Radix sort vs counting sort integrates with other Python features to create powerful, composable solutions

```python
# Example: Radix sort vs counting sort
# This demonstrates the core mechanics of radix sort vs counting sort

def demonstrate_radix_sort_vs_counting_sort():
    """Demonstrate Radix sort vs counting sort with a practical example."""
    # Setup and initialization
    result = None
    print("Running Radix sort vs counting sort demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_radix_sort_vs_counting_sort()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                      Radix Sort                       │
├───────────────────────────────────────────────────────┤
│                 Radix sort algorithm                  │
│                           │                           │
│                           ▼                           │
│                 LSD vs MSD radix sort                 │
│                           │                           │
│                           ▼                           │
│              Digit extraction (by base)               │
│                           │                           │
│                           ▼                           │
│              Stable digit-based sorting               │
│                           │                           │
│                           ▼                           │
│               O(d × (n + k)) complexity               │
│                           │                           │
│                           ▼                           │
│              Base choice and performance              │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with radix sort
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating radix sort
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using Radix sort algorithm
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating Radix sort algorithm."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating radix sort concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- Radix sort algorithm is the foundation of radix sort in Python
- LSD vs MSD radix sort builds on this foundation for more advanced use cases
- Digit extraction (by base) enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding radix sort deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Radix Sort in Python. You learned about Radix sort algorithm, LSD vs MSD radix sort, Digit extraction (by base), Stable digit-based sorting, and
practical techniques for O(d × (n + k)) complexity, Base choice and performance. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
