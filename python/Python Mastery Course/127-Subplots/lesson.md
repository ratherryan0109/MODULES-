# Subplots

## Overview
Create multi-plot layouts with subplots. Arrange multiple charts in grids for comprehensive data dashboards.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply plt.subplot() grid system
2. Understand and apply plt.subplots() for multiple axes
3. Understand and apply figure layout (fig, axes)
4. Understand and apply subplot2grid for irregular grids
5. Understand and apply gridspec for complex layouts
6. Understand and apply shared axis in subplots
7. Understand and apply tight layout for spacing
8. Understand and apply subplots with different sizes
9. Debug common errors and edge cases related to subplots
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **plt.subplot() grid system** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **plt.subplots() for multiple axes** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Figure layout (fig, axes)** | Enables practical implementation | Everyday Python development |
| 4 | **subplot2grid for irregular grids** | Used in real-world applications | Advanced scenarios |
| 5 | **GridSpec for complex layouts** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Shared axis in subplots** | Optimization and best practices | Advanced scenarios |
| 7 | **Tight layout for spacing** | Edge cases and error handling | Everyday Python development |
| 8 | **Subplots with different sizes** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. plt.subplot() grid system

plt.subplot() grid system is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, plt.subplot() grid system follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- plt.subplot() grid system is essential for understanding subplots thoroughly
- Mastery of plt.subplot() grid system enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- plt.subplot() grid system integrates with other Python features to create powerful, composable solutions

```python
# Example: plt.subplot() grid system
# This demonstrates the core mechanics of plt.subplot() grid system

def demonstrate_plt_subplot_grid_system():
    """Demonstrate plt.subplot() grid system with a practical example."""
    # Setup and initialization
    result = None
    print("Running plt.subplot() grid system demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_plt_subplot_grid_system()
```

### 2. plt.subplots() for multiple axes

plt.subplots() for multiple axes is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, plt.subplots() for multiple axes follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- plt.subplots() for multiple axes is essential for understanding subplots thoroughly
- Mastery of plt.subplots() for multiple axes enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- plt.subplots() for multiple axes integrates with other Python features to create powerful, composable solutions

```python
# Example: plt.subplots() for multiple axes
# This demonstrates the core mechanics of plt.subplots() for multiple axes

def demonstrate_plt_subplots_for_multiple_axes():
    """Demonstrate plt.subplots() for multiple axes with a practical example."""
    # Setup and initialization
    result = None
    print("Running plt.subplots() for multiple axes demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_plt_subplots_for_multiple_axes()
```

### 3. Figure layout (fig, axes)

Figure layout (fig, axes) is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, figure layout (fig, axes) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Figure layout (fig, axes) is essential for understanding subplots thoroughly
- Mastery of figure layout (fig, axes) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Figure layout (fig, axes) integrates with other Python features to create powerful, composable solutions

```python
# Example: Figure layout (fig, axes)
# This demonstrates the core mechanics of figure layout (fig, axes)

def demonstrate_figure_layout_fig_axes():
    """Demonstrate Figure layout (fig, axes) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Figure layout (fig, axes) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_figure_layout_fig_axes()
```

### 4. subplot2grid for irregular grids

subplot2grid for irregular grids is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, subplot2grid for irregular grids follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- subplot2grid for irregular grids is essential for understanding subplots thoroughly
- Mastery of subplot2grid for irregular grids enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- subplot2grid for irregular grids integrates with other Python features to create powerful, composable solutions

```python
# Example: subplot2grid for irregular grids
# This demonstrates the core mechanics of subplot2grid for irregular grids

def demonstrate_subplot2grid_for_irregular_grids():
    """Demonstrate subplot2grid for irregular grids with a practical example."""
    # Setup and initialization
    result = None
    print("Running subplot2grid for irregular grids demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_subplot2grid_for_irregular_grids()
```

### 5. GridSpec for complex layouts

GridSpec for complex layouts is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, gridspec for complex layouts follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- GridSpec for complex layouts is essential for understanding subplots thoroughly
- Mastery of gridspec for complex layouts enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- GridSpec for complex layouts integrates with other Python features to create powerful, composable solutions

```python
# Example: GridSpec for complex layouts
# This demonstrates the core mechanics of gridspec for complex layouts

def demonstrate_gridspec_for_complex_layouts():
    """Demonstrate GridSpec for complex layouts with a practical example."""
    # Setup and initialization
    result = None
    print("Running GridSpec for complex layouts demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_gridspec_for_complex_layouts()
```

### 6. Shared axis in subplots

Shared axis in subplots is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, shared axis in subplots follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Shared axis in subplots is essential for understanding subplots thoroughly
- Mastery of shared axis in subplots enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Shared axis in subplots integrates with other Python features to create powerful, composable solutions

```python
# Example: Shared axis in subplots
# This demonstrates the core mechanics of shared axis in subplots

def demonstrate_shared_axis_in_subplots():
    """Demonstrate Shared axis in subplots with a practical example."""
    # Setup and initialization
    result = None
    print("Running Shared axis in subplots demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_shared_axis_in_subplots()
```

### 7. Tight layout for spacing

Tight layout for spacing is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, tight layout for spacing follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Tight layout for spacing is essential for understanding subplots thoroughly
- Mastery of tight layout for spacing enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Tight layout for spacing integrates with other Python features to create powerful, composable solutions

```python
# Example: Tight layout for spacing
# This demonstrates the core mechanics of tight layout for spacing

def demonstrate_tight_layout_for_spacing():
    """Demonstrate Tight layout for spacing with a practical example."""
    # Setup and initialization
    result = None
    print("Running Tight layout for spacing demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_tight_layout_for_spacing()
```

### 8. Subplots with different sizes

Subplots with different sizes is a fundamental concept in Python subplots. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, subplots with different sizes follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Subplots with different sizes is essential for understanding subplots thoroughly
- Mastery of subplots with different sizes enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Subplots with different sizes integrates with other Python features to create powerful, composable solutions

```python
# Example: Subplots with different sizes
# This demonstrates the core mechanics of subplots with different sizes

def demonstrate_subplots_with_different_sizes():
    """Demonstrate Subplots with different sizes with a practical example."""
    # Setup and initialization
    result = None
    print("Running Subplots with different sizes demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_subplots_with_different_sizes()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                       Subplots                        │
├───────────────────────────────────────────────────────┤
│               plt.subplot() grid system               │
│                           │                           │
│                           ▼                           │
│           plt.subplots() for multiple axes            │
│                           │                           │
│                           ▼                           │
│               Figure layout (fig, axes)               │
│                           │                           │
│                           ▼                           │
│           subplot2grid for irregular grids            │
│                           │                           │
│                           ▼                           │
│             GridSpec for complex layouts              │
│                           │                           │
│                           ▼                           │
│                Shared axis in subplots                │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with subplots
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating subplots
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using plt.subplot() grid system
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating plt.subplot() grid system."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating subplots concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- plt.subplot() grid system is the foundation of subplots in Python
- plt.subplots() for multiple axes builds on this foundation for more advanced use cases
- Figure layout (fig, axes) enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding subplots deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Subplots in Python. You learned about plt.subplot() grid system, plt.subplots() for multiple axes, Figure layout (fig, axes), subplot2grid for irregular grids, and
practical techniques for GridSpec for complex layouts, Shared axis in subplots. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
