# Regular Expressions

## Overview
Master regular expressions in Python — pattern matching, search, replace, split, and advanced regex techniques with the re module.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply re.match() vs re.search() vs re.fullmatch()
2. Understand and apply re.findall() and re.finditer()
3. Understand and apply re.sub() for search and replace
4. Understand and apply re.split() advanced splitting
5. Understand and apply character classes and quantifiers
6. Understand and apply groups and backreferences
7. Understand and apply lookahead and lookbehind assertions
8. Understand and apply compiled regex patterns (re.compile())
9. Debug common errors and edge cases related to regular expressions
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **re.match() vs re.search() vs re.fullmatch()** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **re.findall() and re.finditer()** | Builds upon previous concepts | Advanced scenarios |
| 3 | **re.sub() for search and replace** | Enables practical implementation | Everyday Python development |
| 4 | **re.split() advanced splitting** | Used in real-world applications | Advanced scenarios |
| 5 | **Character classes and quantifiers** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Groups and backreferences** | Optimization and best practices | Advanced scenarios |
| 7 | **Lookahead and lookbehind assertions** | Edge cases and error handling | Everyday Python development |
| 8 | **Compiled regex patterns (re.compile())** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. re.match() vs re.search() vs re.fullmatch()

re.match() vs re.search() vs re.fullmatch() is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, re.match() vs re.search() vs re.fullmatch() follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- re.match() vs re.search() vs re.fullmatch() is essential for understanding regular expressions thoroughly
- Mastery of re.match() vs re.search() vs re.fullmatch() enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- re.match() vs re.search() vs re.fullmatch() integrates with other Python features to create powerful, composable solutions

```python
# Example: re.match() vs re.search() vs re.fullmatch()
# This demonstrates the core mechanics of re.match() vs re.search() vs re.fullmatch()

def demonstrate_re_match_vs_re_search_vs_re_fullmatch():
    """Demonstrate re.match() vs re.search() vs re.fullmatch() with a practical example."""
    # Setup and initialization
    result = None
    print("Running re.match() vs re.search() vs re.fullmatch() demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_re_match_vs_re_search_vs_re_fullmatch()
```

### 2. re.findall() and re.finditer()

re.findall() and re.finditer() is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, re.findall() and re.finditer() follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- re.findall() and re.finditer() is essential for understanding regular expressions thoroughly
- Mastery of re.findall() and re.finditer() enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- re.findall() and re.finditer() integrates with other Python features to create powerful, composable solutions

```python
# Example: re.findall() and re.finditer()
# This demonstrates the core mechanics of re.findall() and re.finditer()

def demonstrate_re_findall_and_re_finditer():
    """Demonstrate re.findall() and re.finditer() with a practical example."""
    # Setup and initialization
    result = None
    print("Running re.findall() and re.finditer() demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_re_findall_and_re_finditer()
```

### 3. re.sub() for search and replace

re.sub() for search and replace is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, re.sub() for search and replace follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- re.sub() for search and replace is essential for understanding regular expressions thoroughly
- Mastery of re.sub() for search and replace enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- re.sub() for search and replace integrates with other Python features to create powerful, composable solutions

```python
# Example: re.sub() for search and replace
# This demonstrates the core mechanics of re.sub() for search and replace

def demonstrate_re_sub_for_search_and_replace():
    """Demonstrate re.sub() for search and replace with a practical example."""
    # Setup and initialization
    result = None
    print("Running re.sub() for search and replace demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_re_sub_for_search_and_replace()
```

### 4. re.split() advanced splitting

re.split() advanced splitting is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, re.split() advanced splitting follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- re.split() advanced splitting is essential for understanding regular expressions thoroughly
- Mastery of re.split() advanced splitting enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- re.split() advanced splitting integrates with other Python features to create powerful, composable solutions

```python
# Example: re.split() advanced splitting
# This demonstrates the core mechanics of re.split() advanced splitting

def demonstrate_re_split_advanced_splitting():
    """Demonstrate re.split() advanced splitting with a practical example."""
    # Setup and initialization
    result = None
    print("Running re.split() advanced splitting demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_re_split_advanced_splitting()
```

### 5. Character classes and quantifiers

Character classes and quantifiers is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, character classes and quantifiers follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Character classes and quantifiers is essential for understanding regular expressions thoroughly
- Mastery of character classes and quantifiers enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Character classes and quantifiers integrates with other Python features to create powerful, composable solutions

```python
# Example: Character classes and quantifiers
# This demonstrates the core mechanics of character classes and quantifiers

def demonstrate_character_classes_and_quantifiers():
    """Demonstrate Character classes and quantifiers with a practical example."""
    # Setup and initialization
    result = None
    print("Running Character classes and quantifiers demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_character_classes_and_quantifiers()
```

### 6. Groups and backreferences

Groups and backreferences is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, groups and backreferences follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Groups and backreferences is essential for understanding regular expressions thoroughly
- Mastery of groups and backreferences enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Groups and backreferences integrates with other Python features to create powerful, composable solutions

```python
# Example: Groups and backreferences
# This demonstrates the core mechanics of groups and backreferences

def demonstrate_groups_and_backreferences():
    """Demonstrate Groups and backreferences with a practical example."""
    # Setup and initialization
    result = None
    print("Running Groups and backreferences demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_groups_and_backreferences()
```

### 7. Lookahead and lookbehind assertions

Lookahead and lookbehind assertions is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, lookahead and lookbehind assertions follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Lookahead and lookbehind assertions is essential for understanding regular expressions thoroughly
- Mastery of lookahead and lookbehind assertions enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Lookahead and lookbehind assertions integrates with other Python features to create powerful, composable solutions

```python
# Example: Lookahead and lookbehind assertions
# This demonstrates the core mechanics of lookahead and lookbehind assertions

def demonstrate_lookahead_and_lookbehind_assertions():
    """Demonstrate Lookahead and lookbehind assertions with a practical example."""
    # Setup and initialization
    result = None
    print("Running Lookahead and lookbehind assertions demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_lookahead_and_lookbehind_assertions()
```

### 8. Compiled regex patterns (re.compile())

Compiled regex patterns (re.compile()) is a fundamental concept in Python regular expressions. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, compiled regex patterns (re.compile()) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Compiled regex patterns (re.compile()) is essential for understanding regular expressions thoroughly
- Mastery of compiled regex patterns (re.compile()) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Compiled regex patterns (re.compile()) integrates with other Python features to create powerful, composable solutions

```python
# Example: Compiled regex patterns (re.compile())
# This demonstrates the core mechanics of compiled regex patterns (re.compile())

def demonstrate_compiled_regex_patterns_re_compile():
    """Demonstrate Compiled regex patterns (re.compile()) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Compiled regex patterns (re.compile()) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_compiled_regex_patterns_re_compile()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                  Regular Expressions                  │
├───────────────────────────────────────────────────────┤
│      re.match() vs re.search() vs re.fullmatch()      │
│                           │                           │
│                           ▼                           │
│            re.findall() and re.finditer()             │
│                           │                           │
│                           ▼                           │
│            re.sub() for search and replace            │
│                           │                           │
│                           ▼                           │
│             re.split() advanced splitting             │
│                           │                           │
│                           ▼                           │
│           Character classes and quantifiers           │
│                           │                           │
│                           ▼                           │
│               Groups and backreferences               │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with regular expressions
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating regular expressions
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using re.match() vs re.search() vs re.fullmatch()
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating re.match() vs re.search() vs re.fullmatch()."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating regular expressions concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- re.match() vs re.search() vs re.fullmatch() is the foundation of regular expressions in Python
- re.findall() and re.finditer() builds on this foundation for more advanced use cases
- re.sub() for search and replace enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding regular expressions deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Regular Expressions in Python. You learned about re.match() vs re.search() vs re.fullmatch(), re.findall() and re.finditer(), re.sub() for search and replace, re.split() advanced splitting, and
practical techniques for Character classes and quantifiers, Groups and backreferences. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
