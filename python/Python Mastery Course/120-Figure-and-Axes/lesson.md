# Figure and Axes

## Overview
Understand Matplotlib's object-oriented API — Figure and Axes objects for fine-grained control over every visualization detail.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply figure object (fig = plt.figure())
2. Understand and apply axes object (ax = fig.add_subplot())
3. Understand and apply adding axes (fig.add_axes([l,b,w,h]))
4. Understand and apply figure size and dpi
5. Understand and apply figure layout (constrained, tight)
6. Understand and apply multiple axes in one figure
7. Understand and apply shared axes (sharex, sharey)
8. Understand and apply removing axes spines
9. Debug common errors and edge cases related to figure and axes
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **Figure object (fig = plt.figure())** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **Axes object (ax = fig.add_subplot())** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Adding axes (fig.add_axes([l,b,w,h]))** | Enables practical implementation | Everyday Python development |
| 4 | **Figure size and DPI** | Used in real-world applications | Advanced scenarios |
| 5 | **Figure layout (constrained, tight)** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Multiple axes in one figure** | Optimization and best practices | Advanced scenarios |
| 7 | **Shared axes (sharex, sharey)** | Edge cases and error handling | Everyday Python development |
| 8 | **Removing axes spines** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. Figure object (fig = plt.figure())

Figure object (fig = plt.figure()) is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, figure object (fig = plt.figure()) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Figure object (fig = plt.figure()) is essential for understanding figure and axes thoroughly
- Mastery of figure object (fig = plt.figure()) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Figure object (fig = plt.figure()) integrates with other Python features to create powerful, composable solutions

```python
# Example: Figure object (fig = plt.figure())
# This demonstrates the core mechanics of figure object (fig = plt.figure())

def demonstrate_figure_object_fig_plt_figure():
    """Demonstrate Figure object (fig = plt.figure()) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Figure object (fig = plt.figure()) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_figure_object_fig_plt_figure()
```

### 2. Axes object (ax = fig.add_subplot())

Axes object (ax = fig.add_subplot()) is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, axes object (ax = fig.add_subplot()) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Axes object (ax = fig.add_subplot()) is essential for understanding figure and axes thoroughly
- Mastery of axes object (ax = fig.add_subplot()) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Axes object (ax = fig.add_subplot()) integrates with other Python features to create powerful, composable solutions

```python
# Example: Axes object (ax = fig.add_subplot())
# This demonstrates the core mechanics of axes object (ax = fig.add_subplot())

def demonstrate_axes_object_ax_fig_add_subplot():
    """Demonstrate Axes object (ax = fig.add_subplot()) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Axes object (ax = fig.add_subplot()) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_axes_object_ax_fig_add_subplot()
```

### 3. Adding axes (fig.add_axes([l,b,w,h]))

Adding axes (fig.add_axes([l,b,w,h])) is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, adding axes (fig.add_axes([l,b,w,h])) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Adding axes (fig.add_axes([l,b,w,h])) is essential for understanding figure and axes thoroughly
- Mastery of adding axes (fig.add_axes([l,b,w,h])) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Adding axes (fig.add_axes([l,b,w,h])) integrates with other Python features to create powerful, composable solutions

```python
# Example: Adding axes (fig.add_axes([l,b,w,h]))
# This demonstrates the core mechanics of adding axes (fig.add_axes([l,b,w,h]))

def demonstrate_adding_axes_fig_add_axes_l_b_w_h():
    """Demonstrate Adding axes (fig.add_axes([l,b,w,h])) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Adding axes (fig.add_axes([l,b,w,h])) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_adding_axes_fig_add_axes_l_b_w_h()
```

### 4. Figure size and DPI

Figure size and DPI is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, figure size and dpi follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Figure size and DPI is essential for understanding figure and axes thoroughly
- Mastery of figure size and dpi enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Figure size and DPI integrates with other Python features to create powerful, composable solutions

```python
# Example: Figure size and DPI
# This demonstrates the core mechanics of figure size and dpi

def demonstrate_figure_size_and_dpi():
    """Demonstrate Figure size and DPI with a practical example."""
    # Setup and initialization
    result = None
    print("Running Figure size and DPI demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_figure_size_and_dpi()
```

### 5. Figure layout (constrained, tight)

Figure layout (constrained, tight) is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, figure layout (constrained, tight) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Figure layout (constrained, tight) is essential for understanding figure and axes thoroughly
- Mastery of figure layout (constrained, tight) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Figure layout (constrained, tight) integrates with other Python features to create powerful, composable solutions

```python
# Example: Figure layout (constrained, tight)
# This demonstrates the core mechanics of figure layout (constrained, tight)

def demonstrate_figure_layout_constrained_tight():
    """Demonstrate Figure layout (constrained, tight) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Figure layout (constrained, tight) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_figure_layout_constrained_tight()
```

### 6. Multiple axes in one figure

Multiple axes in one figure is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, multiple axes in one figure follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Multiple axes in one figure is essential for understanding figure and axes thoroughly
- Mastery of multiple axes in one figure enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Multiple axes in one figure integrates with other Python features to create powerful, composable solutions

```python
# Example: Multiple axes in one figure
# This demonstrates the core mechanics of multiple axes in one figure

def demonstrate_multiple_axes_in_one_figure():
    """Demonstrate Multiple axes in one figure with a practical example."""
    # Setup and initialization
    result = None
    print("Running Multiple axes in one figure demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_multiple_axes_in_one_figure()
```

### 7. Shared axes (sharex, sharey)

Shared axes (sharex, sharey) is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, shared axes (sharex, sharey) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Shared axes (sharex, sharey) is essential for understanding figure and axes thoroughly
- Mastery of shared axes (sharex, sharey) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Shared axes (sharex, sharey) integrates with other Python features to create powerful, composable solutions

```python
# Example: Shared axes (sharex, sharey)
# This demonstrates the core mechanics of shared axes (sharex, sharey)

def demonstrate_shared_axes_sharex_sharey():
    """Demonstrate Shared axes (sharex, sharey) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Shared axes (sharex, sharey) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_shared_axes_sharex_sharey()
```

### 8. Removing axes spines

Removing axes spines is a fundamental concept in Python figure and axes. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, removing axes spines follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Removing axes spines is essential for understanding figure and axes thoroughly
- Mastery of removing axes spines enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Removing axes spines integrates with other Python features to create powerful, composable solutions

```python
# Example: Removing axes spines
# This demonstrates the core mechanics of removing axes spines

def demonstrate_removing_axes_spines():
    """Demonstrate Removing axes spines with a practical example."""
    # Setup and initialization
    result = None
    print("Running Removing axes spines demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_removing_axes_spines()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                    Figure and Axes                    │
├───────────────────────────────────────────────────────┤
│          Figure object (fig = plt.figure())           │
│                           │                           │
│                           ▼                           │
│         Axes object (ax = fig.add_subplot())          │
│                           │                           │
│                           ▼                           │
│         Adding axes (fig.add_axes([l,b,w,h]))         │
│                           │                           │
│                           ▼                           │
│                  Figure size and DPI                  │
│                           │                           │
│                           ▼                           │
│          Figure layout (constrained, tight)           │
│                           │                           │
│                           ▼                           │
│              Multiple axes in one figure              │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with figure and axes
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating figure and axes
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using Figure object (fig = plt.figure())
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating Figure object (fig = plt.figure())."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating figure and axes concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- Figure object (fig = plt.figure()) is the foundation of figure and axes in Python
- Axes object (ax = fig.add_subplot()) builds on this foundation for more advanced use cases
- Adding axes (fig.add_axes([l,b,w,h])) enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding figure and axes deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Figure and Axes in Python. You learned about Figure object (fig = plt.figure()), Axes object (ax = fig.add_subplot()), Adding axes (fig.add_axes([l,b,w,h])), Figure size and DPI, and
practical techniques for Figure layout (constrained, tight), Multiple axes in one figure. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
