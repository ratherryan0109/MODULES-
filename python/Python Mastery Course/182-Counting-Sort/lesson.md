# Counting Sort

## Overview
Implement counting sort — O(n+k) non-comparison sorting for integer data. Understand when counting sort beats comparison sorts.

## Learning Objectives
By the end of this module, you will be able to:
1. Understand and apply counting sort algorithm
2. Understand and apply frequency count array
3. Understand and apply cumulative sum (prefix sum)
4. Understand and apply o(n + k) time complexity
5. Understand and apply o(k) space complexity
6. Understand and apply stable counting sort
7. Understand and apply integer range requirements
8. Understand and apply counting sort vs radix sort
9. Debug common errors and edge cases related to counting sort
10. Write production-quality Python code following PEP 8 and best practices

## Key Concepts

| # | Concept | Description | Practical Use |
|---|---------|-------------|---------------|
| 1 | **Counting sort algorithm** | Core foundation for understanding this topic | Everyday Python development |
| 2 | **Frequency count array** | Builds upon previous concepts | Advanced scenarios |
| 3 | **Cumulative sum (prefix sum)** | Enables practical implementation | Everyday Python development |
| 4 | **O(n + k) time complexity** | Used in real-world applications | Advanced scenarios |
| 5 | **O(k) space complexity** | Advanced patterns and techniques | Everyday Python development |
| 6 | **Stable counting sort** | Optimization and best practices | Advanced scenarios |
| 7 | **Integer range requirements** | Edge cases and error handling | Everyday Python development |
| 8 | **Counting sort vs radix sort** | Integration with other Python features | Advanced scenarios |

## Detailed Explanation

### 1. Counting sort algorithm

Counting sort algorithm is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, counting sort algorithm follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Counting sort algorithm is essential for understanding counting sort thoroughly
- Mastery of counting sort algorithm enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Counting sort algorithm integrates with other Python features to create powerful, composable solutions

```python
# Example: Counting sort algorithm
# This demonstrates the core mechanics of counting sort algorithm

def demonstrate_counting_sort_algorithm():
    """Demonstrate Counting sort algorithm with a practical example."""
    # Setup and initialization
    result = None
    print("Running Counting sort algorithm demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_counting_sort_algorithm()
```

### 2. Frequency count array

Frequency count array is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, frequency count array follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Frequency count array is essential for understanding counting sort thoroughly
- Mastery of frequency count array enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Frequency count array integrates with other Python features to create powerful, composable solutions

```python
# Example: Frequency count array
# This demonstrates the core mechanics of frequency count array

def demonstrate_frequency_count_array():
    """Demonstrate Frequency count array with a practical example."""
    # Setup and initialization
    result = None
    print("Running Frequency count array demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_frequency_count_array()
```

### 3. Cumulative sum (prefix sum)

Cumulative sum (prefix sum) is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, cumulative sum (prefix sum) follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Cumulative sum (prefix sum) is essential for understanding counting sort thoroughly
- Mastery of cumulative sum (prefix sum) enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Cumulative sum (prefix sum) integrates with other Python features to create powerful, composable solutions

```python
# Example: Cumulative sum (prefix sum)
# This demonstrates the core mechanics of cumulative sum (prefix sum)

def demonstrate_cumulative_sum_prefix_sum():
    """Demonstrate Cumulative sum (prefix sum) with a practical example."""
    # Setup and initialization
    result = None
    print("Running Cumulative sum (prefix sum) demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_cumulative_sum_prefix_sum()
```

### 4. O(n + k) time complexity

O(n + k) time complexity is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, o(n + k) time complexity follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- O(n + k) time complexity is essential for understanding counting sort thoroughly
- Mastery of o(n + k) time complexity enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- O(n + k) time complexity integrates with other Python features to create powerful, composable solutions

```python
# Example: O(n + k) time complexity
# This demonstrates the core mechanics of o(n + k) time complexity

def demonstrate_o_n_k_time_complexity():
    """Demonstrate O(n + k) time complexity with a practical example."""
    # Setup and initialization
    result = None
    print("Running O(n + k) time complexity demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_o_n_k_time_complexity()
```

### 5. O(k) space complexity

O(k) space complexity is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, o(k) space complexity follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- O(k) space complexity is essential for understanding counting sort thoroughly
- Mastery of o(k) space complexity enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- O(k) space complexity integrates with other Python features to create powerful, composable solutions

```python
# Example: O(k) space complexity
# This demonstrates the core mechanics of o(k) space complexity

def demonstrate_o_k_space_complexity():
    """Demonstrate O(k) space complexity with a practical example."""
    # Setup and initialization
    result = None
    print("Running O(k) space complexity demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_o_k_space_complexity()
```

### 6. Stable counting sort

Stable counting sort is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, stable counting sort follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Stable counting sort is essential for understanding counting sort thoroughly
- Mastery of stable counting sort enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Stable counting sort integrates with other Python features to create powerful, composable solutions

```python
# Example: Stable counting sort
# This demonstrates the core mechanics of stable counting sort

def demonstrate_stable_counting_sort():
    """Demonstrate Stable counting sort with a practical example."""
    # Setup and initialization
    result = None
    print("Running Stable counting sort demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_stable_counting_sort()
```

### 7. Integer range requirements

Integer range requirements is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, integer range requirements follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Integer range requirements is essential for understanding counting sort thoroughly
- Mastery of integer range requirements enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Integer range requirements integrates with other Python features to create powerful, composable solutions

```python
# Example: Integer range requirements
# This demonstrates the core mechanics of integer range requirements

def demonstrate_integer_range_requirements():
    """Demonstrate Integer range requirements with a practical example."""
    # Setup and initialization
    result = None
    print("Running Integer range requirements demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_integer_range_requirements()
```

### 8. Counting sort vs radix sort

Counting sort vs radix sort is a fundamental concept in Python counting sort. It enables developers to write more
efficient, readable, and maintainable code. Understanding this concept deeply is essential for
building robust Python applications that handle real-world scenarios gracefully.

#### How It Works

In Python, counting sort vs radix sort follows specific rules defined by the language specification.
The Python interpreter processes this according to well-defined semantics that ensure
predictable behavior across different environments, operating systems, and Python versions.

#### Key Points
- Counting sort vs radix sort is essential for understanding counting sort thoroughly
- Mastery of counting sort vs radix sort enables more advanced Python programming patterns
- Proper implementation follows PEP 8 conventions and Pythonic idioms
- Common mistakes include misunderstanding scope, type behavior, and edge cases
- Counting sort vs radix sort integrates with other Python features to create powerful, composable solutions

```python
# Example: Counting sort vs radix sort
# This demonstrates the core mechanics of counting sort vs radix sort

def demonstrate_counting_sort_vs_radix_sort():
    """Demonstrate Counting sort vs radix sort with a practical example."""
    # Setup and initialization
    result = None
    print("Running Counting sort vs radix sort demonstration...")
    return result

if __name__ == "__main__":
    demonstrate_counting_sort_vs_radix_sort()
```

## Comparison: Important Techniques

| Technique | When to Use | When to Avoid | Performance Impact |
|-----------|-------------|---------------|-------------------|
| Basic approach | Simple use cases and learning | Complex production scenarios | Minimal overhead |
| Advanced Pythonic pattern | Production code and libraries | Over-engineering simple tasks | Optimal when applicable |
| Functional approach | Data transformations | State-heavy operations | Moderate, often optimal |
| Object-oriented approach | Complex state management | Simple data processing | Moderate overhead |
| Type-hinted approach | Large codebases and teams | Small scripts and prototypes | No runtime impact |
| Comprehension approach | List/dict/set transformations | Complex multi-step logic | Faster than manual loops |

## Common Mistakes and How to Avoid Them

| Mistake | Why It Happens | How to Fix | Prevention |
|---------|---------------|------------|------------|
| Misunderstanding mutability | Reference vs value confusion | Use copy() or deepcopy() | Study Python data model |
| Type errors | Dynamic typing surprises | Use isinstance() and type hints | Add type validation early |
| Mutable default arguments | Default args evaluated once | Use None as sentinel | Never use mutable defaults |
| Indentation errors | Mixing tabs and spaces | Use 4 spaces consistently | Configure editor for PEP 8 |
| Off-by-one errors | Zero-indexing confusion | Check range() boundaries | Test edge cases thoroughly |
| Exception handling gaps | Assuming success paths | Add specific except clauses | Use defensive programming |
| Scope issues | LEGB rule misunderstanding | Use global/nonlocal keywords | Minimize global variables |
| Performance pitfalls | Unoptimized loops/structures | Use list comprehensions | Profile before optimizing |

## Concept Flow Diagram

```
┌───────────────────────────────────────────────────────┐
│                     Counting Sort                     │
├───────────────────────────────────────────────────────┤
│                Counting sort algorithm                │
│                           │                           │
│                           ▼                           │
│                 Frequency count array                 │
│                           │                           │
│                           ▼                           │
│              Cumulative sum (prefix sum)              │
│                           │                           │
│                           ▼                           │
│               O(n + k) time complexity                │
│                           │                           │
│                           ▼                           │
│                 O(k) space complexity                 │
│                           │                           │
│                           ▼                           │
│                 Stable counting sort                  │
└───────────────────────────────────────────────────────┘
```

## Best Practices

1. Follow PEP 8 conventions consistently when working with counting sort
2. Write clear, descriptive variable names that reveal intent
3. Add type hints to improve code readability and enable static analysis
4. Write docstrings for all public functions, classes, and modules (PEP 257)
5. Handle edge cases and error conditions explicitly rather than assuming success
6. Prefer Pythonic idioms: comprehensions, context managers, and generator expressions
7. Test your implementation with different inputs and boundary conditions
8. Profile and optimize only when necessary — avoid premature optimization

## Practical Python Example

```python
# Comprehensive Python example demonstrating counting sort
# This example covers the key concepts discussed in this module

from typing import List, Optional, Any
import sys

# 1. Using Counting sort algorithm
def process_data(items: List[Any]) -> None:
    """Process a list of items demonstrating Counting sort algorithm."""
    if not items:
        print("No items to process")
        return
    for item in items:
        print(f"Processing: {item}")

# 2. Main execution
def main() -> None:
    """Main entry point demonstrating counting sort concepts."""
    sample_data = ["example1", "example2", "example3"]
    process_data(sample_data)

if __name__ == "__main__":
    main()
```

## Practice Exercises

1. **Basic Exercise**: Write a Python script that demonstrates the core concept of this module.
2. **Intermediate Exercise**: Extend the example to handle edge cases and add input validation.
3. **Advanced Exercise**: Combine this module's concepts with Python comprehensions and type hints.
4. **Debugging Exercise**: Intentionally introduce a common bug and practice debugging with pdb.
5. **Refactoring Exercise**: Take non-Pythonic code and refactor it following best practices.
6. **Performance Exercise**: Compare different approaches using timeit and analyze the results.
7. **Testing Exercise**: Write unit tests using pytest for all functions in this module.

## Key Takeaways

- Counting sort algorithm is the foundation of counting sort in Python
- Frequency count array builds on this foundation for more advanced use cases
- Cumulative sum (prefix sum) enables practical implementation in real projects
- Python's philosophy emphasizes readability, simplicity, and explicitness
- Understanding counting sort deeply will make you a more effective Python developer
- Always consider edge cases, performance, and code maintainability

## Summary

This module covered Counting Sort in Python. You learned about Counting sort algorithm, Frequency count array, Cumulative sum (prefix sum), O(n + k) time complexity, and
practical techniques for O(k) space complexity, Stable counting sort. With this knowledge, you can
build more robust and maintainable Python applications. Practice regularly and
refer to the Python documentation for deeper understanding of specific topics.
