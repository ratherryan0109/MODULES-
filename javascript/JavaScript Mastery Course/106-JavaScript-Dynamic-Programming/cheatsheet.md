# JavaScript Dynamic Programming - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Dynamic Programming - key patterns
const memoization__top_down_ = "Memoization (top-down)";
const result = memoization__top_down_.toUpperCase();
console.log(result); // "MEMOIZATION (TOP-DOWN)"
```

### Key Concepts

- **Memoization (top-down)**: Core concept in javascript dynamic programming
- **Tabulation (bottom-up)**: Core concept in javascript dynamic programming
- **Fibonacci with DP**: Core concept in javascript dynamic programming
- **Knapsack problem**: Core concept in javascript dynamic programming
- **Longest common subsequence**: Core concept in javascript dynamic programming
- **Longest increasing subsequence**: Core concept in javascript dynamic programming
- **Coin change problem**: Core concept in javascript dynamic programming
- **Edit distance**: Core concept in javascript dynamic programming

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Dynamic Programming"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Dynamic Programming"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Memoization (top-down)
- Tabulation (bottom-up)
- Fibonacci with DP
- Knapsack problem
- Longest common subsequence
- Longest increasing subsequence
- Coin change problem
- Edit distance
