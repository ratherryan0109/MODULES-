# JavaScript Currying - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Currying - key patterns
const currying_definition = "Currying definition";
const result = currying_definition.toUpperCase();
console.log(result); // "CURRYING DEFINITION"
```

### Key Concepts

- **Currying definition**: Core concept in javascript currying
- **Manual currying implementation**: Core concept in javascript currying
- **Currying with bind()**: Core concept in javascript currying
- **Currying with closures**: Core concept in javascript currying
- **Partial application vs currying**: Core concept in javascript currying
- **Currying in functional libraries**: Core concept in javascript currying
- **Advanced currying patterns**: Core concept in javascript currying
- **When to use currying**: Core concept in javascript currying

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Currying"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Currying"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Currying definition
- Manual currying implementation
- Currying with bind()
- Currying with closures
- Partial application vs currying
- Currying in functional libraries
- Advanced currying patterns
- When to use currying
