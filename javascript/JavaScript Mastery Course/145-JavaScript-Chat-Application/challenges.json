[
  {
    "id": "145-challenge-1",
    "title": "Basic JavaScript Chat Application",
    "description": "Write a function that demonstrates the core concepts of JavaScript Chat Application. Focus on implementing Socket.IO setup correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_145() {\n  // Implement socket.io setup\n  // Return a meaningful result\n  return \"Implement JavaScript Chat Application\";\n}",
    "solution": "function basic_145() {\n  const concepts = [\"Socket.IO setup\", \"Real-time messaging\", \"User authentication\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "145-challenge-2",
    "title": "Intermediate JavaScript Chat Application",
    "description": "Create a function that uses Socket.IO setup and Real-time messaging together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_145(input) {\n  // Process input using socket.io setup\n  // and real-time messaging\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_145(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "145-challenge-3",
    "title": "Advanced JavaScript Chat Application Project",
    "description": "Build a small application that applies Socket.IO setup, Real-time messaging, User authentication in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptChatApplication {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using socket.io setup\n  }\n}",
    "solution": "class JavaScriptChatApplication {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "145-challenge-4",
    "title": "Debugging JavaScript Chat Application",
    "description": "The following code has bugs related to Socket.IO setup. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData145(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData145([\"a\", \"b\", \"c\"]));",
    "solution": "function processData145(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData145([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "145-challenge-5",
    "title": "Refactoring JavaScript Chat Application",
    "description": "Refactor the following code to use modern JavaScript patterns with Socket.IO setup and Real-time messaging.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay145(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored145(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]