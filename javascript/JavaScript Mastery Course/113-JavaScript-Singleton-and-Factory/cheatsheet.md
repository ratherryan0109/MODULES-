# JavaScript Singleton and Factory - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Singleton and Factory - key patterns
const singleton_with_module_pattern = "Singleton with module pattern";
const result = singleton_with_module_pattern.toUpperCase();
console.log(result); // "SINGLETON WITH MODULE PATTERN"
```

### Key Concepts

- **Singleton with module pattern**: Core concept in javascript singleton and factory
- **Singleton with class**: Core concept in javascript singleton and factory
- **Lazy initialization**: Core concept in javascript singleton and factory
- **Simple factory**: Core concept in javascript singleton and factory
- **Factory method**: Core concept in javascript singleton and factory
- **Abstract factory**: Core concept in javascript singleton and factory
- **Factory vs constructor**: Core concept in javascript singleton and factory
- **Modern singleton alternatives**: Core concept in javascript singleton and factory

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Singleton and Factory"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Singleton and Factory"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Singleton with module pattern
- Singleton with class
- Lazy initialization
- Simple factory
- Factory method
- Abstract factory
- Factory vs constructor
- Modern singleton alternatives
