# JavaScript Array Methods Part 2 - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Array Methods Part 2 - key patterns
const foreach_for_iteration = "forEach for iteration";
const result = foreach_for_iteration.toUpperCase();
console.log(result); // "FOREACH FOR ITERATION"
```

### Key Concepts

- **forEach for iteration**: Core concept in javascript array methods part 2
- **map for transformation**: Core concept in javascript array methods part 2
- **filter for selection**: Core concept in javascript array methods part 2
- **reduce for accumulation**: Core concept in javascript array methods part 2
- **every and some for testing**: Core concept in javascript array methods part 2
- **find and findIndex**: Core concept in javascript array methods part 2
- **Array method chaining**: Core concept in javascript array methods part 2
- **flat and flatMap**: Core concept in javascript array methods part 2

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Array Methods Part 2"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Array Methods Part 2"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- forEach for iteration
- map for transformation
- filter for selection
- reduce for accumulation
- every and some for testing
- find and findIndex
- Array method chaining
- flat and flatMap
