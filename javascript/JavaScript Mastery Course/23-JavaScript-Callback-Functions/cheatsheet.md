# JavaScript Callback Functions - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Callback Functions - key patterns
const callback_function_definition = "Callback function definition";
const result = callback_function_definition.toUpperCase();
console.log(result); // "CALLBACK FUNCTION DEFINITION"
```

### Key Concepts

- **Callback function definition**: Core concept in javascript callback functions
- **Higher-order functions**: Core concept in javascript callback functions
- **Synchronous callbacks**: Core concept in javascript callback functions
- **Asynchronous callbacks**: Core concept in javascript callback functions
- **Callback hell**: Core concept in javascript callback functions
- **Named vs anonymous callbacks**: Core concept in javascript callback functions
- **Error-first callbacks**: Core concept in javascript callback functions
- **Callback patterns in array methods**: Core concept in javascript callback functions

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Callback Functions"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Callback Functions"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Callback function definition
- Higher-order functions
- Synchronous callbacks
- Asynchronous callbacks
- Callback hell
- Named vs anonymous callbacks
- Error-first callbacks
- Callback patterns in array methods
