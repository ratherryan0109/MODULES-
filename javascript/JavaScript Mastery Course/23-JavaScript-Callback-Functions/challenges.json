[
  {
    "id": "23-challenge-1",
    "title": "Basic JavaScript Callback Functions",
    "description": "Write a function that demonstrates the core concepts of JavaScript Callback Functions. Focus on implementing Callback function definition correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_23() {\n  // Implement callback function definition\n  // Return a meaningful result\n  return \"Implement JavaScript Callback Functions\";\n}",
    "solution": "function basic_23() {\n  const concepts = [\"Callback function definition\", \"Higher-order functions\", \"Synchronous callbacks\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "23-challenge-2",
    "title": "Intermediate JavaScript Callback Functions",
    "description": "Create a function that uses Callback function definition and Higher-order functions together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_23(input) {\n  // Process input using callback function definition\n  // and higher-order functions\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_23(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "23-challenge-3",
    "title": "Advanced JavaScript Callback Functions Project",
    "description": "Build a small application that applies Callback function definition, Higher-order functions, Synchronous callbacks in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptCallbackFunctions {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using callback function definition\n  }\n}",
    "solution": "class JavaScriptCallbackFunctions {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "23-challenge-4",
    "title": "Debugging JavaScript Callback Functions",
    "description": "The following code has bugs related to Callback function definition. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData23(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData23([\"a\", \"b\", \"c\"]));",
    "solution": "function processData23(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData23([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "23-challenge-5",
    "title": "Refactoring JavaScript Callback Functions",
    "description": "Refactor the following code to use modern JavaScript patterns with Callback function definition and Higher-order functions.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay23(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored23(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]