# JavaScript Arrays Basics - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Arrays Basics - key patterns
const array_creation__literal__constructor__array_from_ = "Array creation (literal, constructor, Array.from)";
const result = array_creation__literal__constructor__array_from_.toUpperCase();
console.log(result); // "ARRAY CREATION (LITERAL, CONSTRUCTOR, ARRAY.FROM)"
```

### Key Concepts

- **Array creation (literal, constructor, Array.from)**: Core concept in javascript arrays basics
- **Array indexing and length**: Core concept in javascript arrays basics
- **Accessing and modifying elements**: Core concept in javascript arrays basics
- **Array detection (isArray)**: Core concept in javascript arrays basics
- **Array-like objects**: Core concept in javascript arrays basics
- **Sparse arrays**: Core concept in javascript arrays basics
- **Multidimensional arrays**: Core concept in javascript arrays basics
- **Array destructuring basics**: Core concept in javascript arrays basics

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Arrays Basics"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Arrays Basics"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Array creation (literal, constructor, Array.from)
- Array indexing and length
- Accessing and modifying elements
- Array detection (isArray)
- Array-like objects
- Sparse arrays
- Multidimensional arrays
- Array destructuring basics
