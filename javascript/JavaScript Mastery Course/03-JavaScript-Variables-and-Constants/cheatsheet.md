# JavaScript Variables and Constants - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Variables and Constants - key patterns
const var__let__const_differences = "var, let, const differences";
const result = var__let__const_differences.toUpperCase();
console.log(result); // "VAR, LET, CONST DIFFERENCES"
```

### Key Concepts

- **var, let, const differences**: Core concept in javascript variables and constants
- **Block scoping with let and const**: Core concept in javascript variables and constants
- **Variable hoisting**: Core concept in javascript variables and constants
- **Temporal Dead Zone (TDZ)**: Core concept in javascript variables and constants
- **Naming conventions (camelCase)**: Core concept in javascript variables and constants
- **Constants and immutability**: Core concept in javascript variables and constants
- **Global vs local variables**: Core concept in javascript variables and constants
- **Variable shadowing**: Core concept in javascript variables and constants

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Variables and Constants"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Variables and Constants"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- var, let, const differences
- Block scoping with let and const
- Variable hoisting
- Temporal Dead Zone (TDZ)
- Naming conventions (camelCase)
- Constants and immutability
- Global vs local variables
- Variable shadowing
