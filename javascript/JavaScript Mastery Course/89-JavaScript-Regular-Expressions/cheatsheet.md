# JavaScript Regular Expressions - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Regular Expressions - key patterns
const regex_creation__literal__constructor_ = "Regex creation (literal, constructor)";
const result = regex_creation__literal__constructor_.toUpperCase();
console.log(result); // "REGEX CREATION (LITERAL, CONSTRUCTOR)"
```

### Key Concepts

- **Regex creation (literal, constructor)**: Core concept in javascript regular expressions
- **Patterns (character classes, quantifiers)**: Core concept in javascript regular expressions
- **Groups and backreferences**: Core concept in javascript regular expressions
- **Lookahead and lookbehind**: Core concept in javascript regular expressions
- **Regex flags (g, i, m, s, u, y)**: Core concept in javascript regular expressions
- **exec and test methods**: Core concept in javascript regular expressions
- **String methods with regex**: Core concept in javascript regular expressions
- **Regex performance and optimization**: Core concept in javascript regular expressions

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Regular Expressions"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Regular Expressions"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Regex creation (literal, constructor)
- Patterns (character classes, quantifiers)
- Groups and backreferences
- Lookahead and lookbehind
- Regex flags (g, i, m, s, u, y)
- exec and test methods
- String methods with regex
- Regex performance and optimization
