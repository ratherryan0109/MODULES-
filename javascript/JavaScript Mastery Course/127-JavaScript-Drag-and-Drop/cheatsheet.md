# JavaScript Drag and Drop - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Drag and Drop - key patterns
const dragstart__dragover__drop_events = "dragstart, dragover, drop events";
const result = dragstart__dragover__drop_events.toUpperCase();
console.log(result); // "DRAGSTART, DRAGOVER, DROP EVENTS"
```

### Key Concepts

- **dragstart, dragover, drop events**: Core concept in javascript drag and drop
- **DataTransfer API**: Core concept in javascript drag and drop
- **Draggable attribute**: Core concept in javascript drag and drop
- **Custom drag images**: Core concept in javascript drag and drop
- **Sortable list implementation**: Core concept in javascript drag and drop
- **File drag and drop**: Core concept in javascript drag and drop
- **Touch events for mobile drag**: Core concept in javascript drag and drop
- **Drag and drop libraries**: Core concept in javascript drag and drop

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Drag and Drop"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Drag and Drop"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- dragstart, dragover, drop events
- DataTransfer API
- Draggable attribute
- Custom drag images
- Sortable list implementation
- File drag and drop
- Touch events for mobile drag
- Drag and drop libraries
