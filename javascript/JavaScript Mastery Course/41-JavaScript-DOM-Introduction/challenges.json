[
  {
    "id": "41-challenge-1",
    "title": "Basic JavaScript DOM Introduction",
    "description": "Write a function that demonstrates the core concepts of JavaScript DOM Introduction. Focus on implementing What is the DOM correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_41() {\n  // Implement what is the dom\n  // Return a meaningful result\n  return \"Implement JavaScript DOM Introduction\";\n}",
    "solution": "function basic_41() {\n  const concepts = [\"What is the DOM\", \"DOM tree structure\", \"document object\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "41-challenge-2",
    "title": "Intermediate JavaScript DOM Introduction",
    "description": "Create a function that uses What is the DOM and DOM tree structure together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_41(input) {\n  // Process input using what is the dom\n  // and dom tree structure\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_41(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "41-challenge-3",
    "title": "Advanced JavaScript DOM Introduction Project",
    "description": "Build a small application that applies What is the DOM, DOM tree structure, document object in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptDOMIntroduction {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using what is the dom\n  }\n}",
    "solution": "class JavaScriptDOMIntroduction {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "41-challenge-4",
    "title": "Debugging JavaScript DOM Introduction",
    "description": "The following code has bugs related to What is the DOM. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData41(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData41([\"a\", \"b\", \"c\"]));",
    "solution": "function processData41(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData41([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "41-challenge-5",
    "title": "Refactoring JavaScript DOM Introduction",
    "description": "Refactor the following code to use modern JavaScript patterns with What is the DOM and DOM tree structure.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay41(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored41(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]