[
  {
    "id": "134-challenge-1",
    "title": "Basic JavaScript Tools and Builders",
    "description": "Write a function that demonstrates the core concepts of JavaScript Tools and Builders. Focus on implementing Webpack configuration correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_134() {\n  // Implement webpack configuration\n  // Return a meaningful result\n  return \"Implement JavaScript Tools and Builders\";\n}",
    "solution": "function basic_134() {\n  const concepts = [\"Webpack configuration\", \"Vite bundler\", \"Babel transpilation\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "134-challenge-2",
    "title": "Intermediate JavaScript Tools and Builders",
    "description": "Create a function that uses Webpack configuration and Vite bundler together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_134(input) {\n  // Process input using webpack configuration\n  // and vite bundler\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_134(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "134-challenge-3",
    "title": "Advanced JavaScript Tools and Builders Project",
    "description": "Build a small application that applies Webpack configuration, Vite bundler, Babel transpilation in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptToolsandBuilders {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using webpack configuration\n  }\n}",
    "solution": "class JavaScriptToolsandBuilders {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "134-challenge-4",
    "title": "Debugging JavaScript Tools and Builders",
    "description": "The following code has bugs related to Webpack configuration. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData134(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData134([\"a\", \"b\", \"c\"]));",
    "solution": "function processData134(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData134([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "134-challenge-5",
    "title": "Refactoring JavaScript Tools and Builders",
    "description": "Refactor the following code to use modern JavaScript patterns with Webpack configuration and Vite bundler.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay134(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored134(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]