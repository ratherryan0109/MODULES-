# JavaScript Whiteboard App - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Whiteboard App - key patterns
const canvas_drawing_tools = "Canvas drawing tools";
const result = canvas_drawing_tools.toUpperCase();
console.log(result); // "CANVAS DRAWING TOOLS"
```

### Key Concepts

- **Canvas drawing tools**: Core concept in javascript whiteboard app
- **Shape tools (rectangle, circle, line)**: Core concept in javascript whiteboard app
- **Color picker and brush size**: Core concept in javascript whiteboard app
- **Sticky notes**: Core concept in javascript whiteboard app
- **Undo/redo system**: Core concept in javascript whiteboard app
- **Clear and erase**: Core concept in javascript whiteboard app
- **Export as image**: Core concept in javascript whiteboard app
- **Real-time collaboration basics**: Core concept in javascript whiteboard app

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Whiteboard App"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Whiteboard App"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Canvas drawing tools
- Shape tools (rectangle, circle, line)
- Color picker and brush size
- Sticky notes
- Undo/redo system
- Clear and erase
- Export as image
- Real-time collaboration basics
