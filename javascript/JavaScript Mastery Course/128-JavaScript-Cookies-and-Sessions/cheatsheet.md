# JavaScript Cookies and Sessions - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Cookies and Sessions - key patterns
const document_cookie_api = "document.cookie API";
const result = document_cookie_api.toUpperCase();
console.log(result); // "DOCUMENT.COOKIE API"
```

### Key Concepts

- **document.cookie API**: Core concept in javascript cookies and sessions
- **Cookie attributes (expires, path, domain)**: Core concept in javascript cookies and sessions
- **Secure and HttpOnly cookies**: Core concept in javascript cookies and sessions
- **SameSite attribute**: Core concept in javascript cookies and sessions
- **Cookie vs sessionStorage vs localStorage**: Core concept in javascript cookies and sessions
- **Session management patterns**: Core concept in javascript cookies and sessions
- **Cookie consent and GDPR**: Core concept in javascript cookies and sessions
- **Third-party cookies**: Core concept in javascript cookies and sessions

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Cookies and Sessions"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Cookies and Sessions"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- document.cookie API
- Cookie attributes (expires, path, domain)
- Secure and HttpOnly cookies
- SameSite attribute
- Cookie vs sessionStorage vs localStorage
- Session management patterns
- Cookie consent and GDPR
- Third-party cookies
