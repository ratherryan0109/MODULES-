# JavaScript Loops: while and do/while - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Loops: while and do/while - key patterns
const while_loop_syntax = "while loop syntax";
const result = while_loop_syntax.toUpperCase();
console.log(result); // "WHILE LOOP SYNTAX"
```

### Key Concepts

- **while loop syntax**: Core concept in javascript loops: while and do/while
- **do/while loop syntax**: Core concept in javascript loops: while and do/while
- **Loop condition evaluation**: Core concept in javascript loops: while and do/while
- **Infinite loops prevention**: Core concept in javascript loops: while and do/while
- **break statement**: Core concept in javascript loops: while and do/while
- **continue statement**: Core concept in javascript loops: while and do/while
- **Difference between while and do/while**: Core concept in javascript loops: while and do/while
- **Loop use cases**: Core concept in javascript loops: while and do/while

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Loops: while and do/while"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Loops: while and do/while"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- while loop syntax
- do/while loop syntax
- Loop condition evaluation
- Infinite loops prevention
- break statement
- continue statement
- Difference between while and do/while
- Loop use cases
