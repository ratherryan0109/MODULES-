[
  {
    "id": "137-challenge-1",
    "title": "Basic JavaScript React Basics",
    "description": "Write a function that demonstrates the core concepts of JavaScript React Basics. Focus on implementing React setup (Vite + React) correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_137() {\n  // Implement react setup (vite + react)\n  // Return a meaningful result\n  return \"Implement JavaScript React Basics\";\n}",
    "solution": "function basic_137() {\n  const concepts = [\"React setup (Vite + React)\", \"JSX syntax\", \"Components (functional)\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "137-challenge-2",
    "title": "Intermediate JavaScript React Basics",
    "description": "Create a function that uses React setup (Vite + React) and JSX syntax together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_137(input) {\n  // Process input using react setup (vite + react)\n  // and jsx syntax\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_137(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "137-challenge-3",
    "title": "Advanced JavaScript React Basics Project",
    "description": "Build a small application that applies React setup (Vite + React), JSX syntax, Components (functional) in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptReactBasics {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using react setup (vite + react)\n  }\n}",
    "solution": "class JavaScriptReactBasics {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "137-challenge-4",
    "title": "Debugging JavaScript React Basics",
    "description": "The following code has bugs related to React setup (Vite + React). Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData137(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData137([\"a\", \"b\", \"c\"]));",
    "solution": "function processData137(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData137([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "137-challenge-5",
    "title": "Refactoring JavaScript React Basics",
    "description": "Refactor the following code to use modern JavaScript patterns with React setup (Vite + React) and JSX syntax.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay137(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored137(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]