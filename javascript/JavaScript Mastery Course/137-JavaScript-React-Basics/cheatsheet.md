# JavaScript React Basics - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript React Basics - key patterns
const react_setup__vite___react_ = "React setup (Vite + React)";
const result = react_setup__vite___react_.toUpperCase();
console.log(result); // "REACT SETUP (VITE + REACT)"
```

### Key Concepts

- **React setup (Vite + React)**: Core concept in javascript react basics
- **JSX syntax**: Core concept in javascript react basics
- **Components (functional)**: Core concept in javascript react basics
- **Props and prop drilling**: Core concept in javascript react basics
- **useState hook**: Core concept in javascript react basics
- **useEffect hook**: Core concept in javascript react basics
- **Event handling in React**: Core concept in javascript react basics
- **Conditional rendering**: Core concept in javascript react basics

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript React Basics"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript React Basics"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- React setup (Vite + React)
- JSX syntax
- Components (functional)
- Props and prop drilling
- useState hook
- useEffect hook
- Event handling in React
- Conditional rendering
