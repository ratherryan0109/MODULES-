# JavaScript Recursion and Backtracking - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Recursion and Backtracking - key patterns
const recursion_patterns = "Recursion patterns";
const result = recursion_patterns.toUpperCase();
console.log(result); // "RECURSION PATTERNS"
```

### Key Concepts

- **Recursion patterns**: Core concept in javascript recursion and backtracking
- **Recursive backtracking**: Core concept in javascript recursion and backtracking
- **N-Queens problem**: Core concept in javascript recursion and backtracking
- **Permutations generation**: Core concept in javascript recursion and backtracking
- **Subsets and combinations**: Core concept in javascript recursion and backtracking
- **Maze solving**: Core concept in javascript recursion and backtracking
- **Sudoku solver**: Core concept in javascript recursion and backtracking
- **Memoization in recursion**: Core concept in javascript recursion and backtracking

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Recursion and Backtracking"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Recursion and Backtracking"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Recursion patterns
- Recursive backtracking
- N-Queens problem
- Permutations generation
- Subsets and combinations
- Maze solving
- Sudoku solver
- Memoization in recursion
