# JavaScript Recursion - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Recursion - key patterns
const recursive_function_definition = "Recursive function definition";
const result = recursive_function_definition.toUpperCase();
console.log(result); // "RECURSIVE FUNCTION DEFINITION"
```

### Key Concepts

- **Recursive function definition**: Core concept in javascript recursion
- **Base case and recursive case**: Core concept in javascript recursion
- **Call stack in recursion**: Core concept in javascript recursion
- **Recursion vs iteration**: Core concept in javascript recursion
- **Tail recursion**: Core concept in javascript recursion
- **Recursive array traversal**: Core concept in javascript recursion
- **Recursive tree traversal**: Core concept in javascript recursion
- **Depth-first search recursion**: Core concept in javascript recursion

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Recursion"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Recursion"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Recursive function definition
- Base case and recursive case
- Call stack in recursion
- Recursion vs iteration
- Tail recursion
- Recursive array traversal
- Recursive tree traversal
- Depth-first search recursion
