[
  {
    "id": "70-challenge-1",
    "title": "Basic JavaScript CommonJS vs ESM",
    "description": "Write a function that demonstrates the core concepts of JavaScript CommonJS vs ESM. Focus on implementing require() syntax correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_70() {\n  // Implement require() syntax\n  // Return a meaningful result\n  return \"Implement JavaScript CommonJS vs ESM\";\n}",
    "solution": "function basic_70() {\n  const concepts = [\"require() syntax\", \"module.exports\", \"import/export ES modules\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "70-challenge-2",
    "title": "Intermediate JavaScript CommonJS vs ESM",
    "description": "Create a function that uses require() syntax and module.exports together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_70(input) {\n  // Process input using require() syntax\n  // and module.exports\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_70(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "70-challenge-3",
    "title": "Advanced JavaScript CommonJS vs ESM Project",
    "description": "Build a small application that applies require() syntax, module.exports, import/export ES modules in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptCommonJSvsESM {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using require() syntax\n  }\n}",
    "solution": "class JavaScriptCommonJSvsESM {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "70-challenge-4",
    "title": "Debugging JavaScript CommonJS vs ESM",
    "description": "The following code has bugs related to require() syntax. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData70(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData70([\"a\", \"b\", \"c\"]));",
    "solution": "function processData70(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData70([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "70-challenge-5",
    "title": "Refactoring JavaScript CommonJS vs ESM",
    "description": "Refactor the following code to use modern JavaScript patterns with require() syntax and module.exports.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay70(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored70(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]