# JavaScript Fetch API - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Fetch API - key patterns
const fetch___function = "fetch() function";
const result = fetch___function.toUpperCase();
console.log(result); // "FETCH() FUNCTION"
```

### Key Concepts

- **fetch() function**: Core concept in javascript fetch api
- **Response object (json, text, blob)**: Core concept in javascript fetch api
- **Request object**: Core concept in javascript fetch api
- **Headers object**: Core concept in javascript fetch api
- **HTTP methods (GET, POST, PUT, DELETE)**: Core concept in javascript fetch api
- **Request options**: Core concept in javascript fetch api
- **CORS and credentials**: Core concept in javascript fetch api
- **Error handling with fetch**: Core concept in javascript fetch api

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Fetch API"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Fetch API"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- fetch() function
- Response object (json, text, blob)
- Request object
- Headers object
- HTTP methods (GET, POST, PUT, DELETE)
- Request options
- CORS and credentials
- Error handling with fetch
