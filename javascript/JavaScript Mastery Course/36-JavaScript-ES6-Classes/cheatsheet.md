# JavaScript ES6 Classes - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript ES6 Classes - key patterns
const class_keyword = "class keyword";
const result = class_keyword.toUpperCase();
console.log(result); // "CLASS KEYWORD"
```

### Key Concepts

- **class keyword**: Core concept in javascript es6 classes
- **constructor method**: Core concept in javascript es6 classes
- **Instance methods**: Core concept in javascript es6 classes
- **Static methods and properties**: Core concept in javascript es6 classes
- **Class inheritance (extends)**: Core concept in javascript es6 classes
- **super keyword**: Core concept in javascript es6 classes
- **Private class fields (#)**: Core concept in javascript es6 classes
- **Public class fields**: Core concept in javascript es6 classes

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript ES6 Classes"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript ES6 Classes"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- class keyword
- constructor method
- Instance methods
- Static methods and properties
- Class inheritance (extends)
- super keyword
- Private class fields (#)
- Public class fields
