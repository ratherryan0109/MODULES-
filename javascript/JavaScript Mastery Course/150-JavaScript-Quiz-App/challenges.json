[
  {
    "id": "150-challenge-1",
    "title": "Basic JavaScript Quiz App",
    "description": "Write a function that demonstrates the core concepts of JavaScript Quiz App. Focus on implementing Quiz engine architecture correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_150() {\n  // Implement quiz engine architecture\n  // Return a meaningful result\n  return \"Implement JavaScript Quiz App\";\n}",
    "solution": "function basic_150() {\n  const concepts = [\"Quiz engine architecture\", \"Timer countdown\", \"Scoring system\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "150-challenge-2",
    "title": "Intermediate JavaScript Quiz App",
    "description": "Create a function that uses Quiz engine architecture and Timer countdown together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_150(input) {\n  // Process input using quiz engine architecture\n  // and timer countdown\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_150(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "150-challenge-3",
    "title": "Advanced JavaScript Quiz App Project",
    "description": "Build a small application that applies Quiz engine architecture, Timer countdown, Scoring system in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptQuizApp {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using quiz engine architecture\n  }\n}",
    "solution": "class JavaScriptQuizApp {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "150-challenge-4",
    "title": "Debugging JavaScript Quiz App",
    "description": "The following code has bugs related to Quiz engine architecture. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData150(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData150([\"a\", \"b\", \"c\"]));",
    "solution": "function processData150(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData150([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "150-challenge-5",
    "title": "Refactoring JavaScript Quiz App",
    "description": "Refactor the following code to use modern JavaScript patterns with Quiz engine architecture and Timer countdown.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay150(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored150(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]