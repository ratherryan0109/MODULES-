# JavaScript Image Editor - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Image Editor - key patterns
const canvas_image_loading = "Canvas image loading";
const result = canvas_image_loading.toUpperCase();
console.log(result); // "CANVAS IMAGE LOADING"
```

### Key Concepts

- **Canvas image loading**: Core concept in javascript image editor
- **Image filters (brightness, contrast, sepia)**: Core concept in javascript image editor
- **Drawing tools (brush, eraser, shapes)**: Core concept in javascript image editor
- **Crop and resize**: Core concept in javascript image editor
- **Undo/redo feature**: Core concept in javascript image editor
- **Layer management**: Core concept in javascript image editor
- **Image export (download)**: Core concept in javascript image editor
- **File save as PNG/JPEG**: Core concept in javascript image editor

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Image Editor"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Image Editor"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Canvas image loading
- Image filters (brightness, contrast, sepia)
- Drawing tools (brush, eraser, shapes)
- Crop and resize
- Undo/redo feature
- Layer management
- Image export (download)
- File save as PNG/JPEG
