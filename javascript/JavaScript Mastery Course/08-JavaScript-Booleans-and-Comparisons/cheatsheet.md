# JavaScript Booleans and Comparisons - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Booleans and Comparisons - key patterns
const boolean_primitive_and_object = "Boolean primitive and object";
const result = boolean_primitive_and_object.toUpperCase();
console.log(result); // "BOOLEAN PRIMITIVE AND OBJECT"
```

### Key Concepts

- **Boolean primitive and object**: Core concept in javascript booleans and comparisons
- **Truthy and falsy values**: Core concept in javascript booleans and comparisons
- **Equality (== vs ===)**: Core concept in javascript booleans and comparisons
- **Relational operators**: Core concept in javascript booleans and comparisons
- **Conditional logic**: Core concept in javascript booleans and comparisons
- **Boolean coercion**: Core concept in javascript booleans and comparisons
- **Object.is() comparison**: Core concept in javascript booleans and comparisons
- **Comparing objects**: Core concept in javascript booleans and comparisons

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Booleans and Comparisons"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Booleans and Comparisons"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Boolean primitive and object
- Truthy and falsy values
- Equality (== vs ===)
- Relational operators
- Conditional logic
- Boolean coercion
- Object.is() comparison
- Comparing objects
