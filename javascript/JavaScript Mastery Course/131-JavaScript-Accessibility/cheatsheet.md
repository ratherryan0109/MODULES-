# JavaScript Accessibility (a11y) - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Accessibility (a11y) - key patterns
const aria_roles_and_attributes = "ARIA roles and attributes";
const result = aria_roles_and_attributes.toUpperCase();
console.log(result); // "ARIA ROLES AND ATTRIBUTES"
```

### Key Concepts

- **ARIA roles and attributes**: Core concept in javascript accessibility (a11y)
- **Keyboard navigation**: Core concept in javascript accessibility (a11y)
- **Focus management**: Core concept in javascript accessibility (a11y)
- **Screen reader compatibility**: Core concept in javascript accessibility (a11y)
- **WCAG 2.1 guidelines**: Core concept in javascript accessibility (a11y)
- **Semantic HTML with JS**: Core concept in javascript accessibility (a11y)
- **Accessible forms**: Core concept in javascript accessibility (a11y)
- **Accessibility testing tools**: Core concept in javascript accessibility (a11y)

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Accessibility (a11y)"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Accessibility (a11y)"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- ARIA roles and attributes
- Keyboard navigation
- Focus management
- Screen reader compatibility
- WCAG 2.1 guidelines
- Semantic HTML with JS
- Accessible forms
- Accessibility testing tools
