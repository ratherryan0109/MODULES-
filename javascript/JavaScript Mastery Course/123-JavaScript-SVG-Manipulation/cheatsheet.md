# JavaScript SVG Manipulation - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript SVG Manipulation - key patterns
const svg_elements__circle__rect__path__text_ = "SVG elements (circle, rect, path, text)";
const result = svg_elements__circle__rect__path__text_.toUpperCase();
console.log(result); // "SVG ELEMENTS (CIRCLE, RECT, PATH, TEXT)"
```

### Key Concepts

- **SVG elements (circle, rect, path, text)**: Core concept in javascript svg manipulation
- **SVG attributes vs CSS**: Core concept in javascript svg manipulation
- **SVG viewBox and coordinates**: Core concept in javascript svg manipulation
- **JavaScript SVG manipulation**: Core concept in javascript svg manipulation
- **SVG animation with SMIL/CSS**: Core concept in javascript svg manipulation
- **Interactive SVGs**: Core concept in javascript svg manipulation
- **Data-driven SVGs**: Core concept in javascript svg manipulation
- **SVG vs Canvas comparison**: Core concept in javascript svg manipulation

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript SVG Manipulation"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript SVG Manipulation"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- SVG elements (circle, rect, path, text)
- SVG attributes vs CSS
- SVG viewBox and coordinates
- JavaScript SVG manipulation
- SVG animation with SMIL/CSS
- Interactive SVGs
- Data-driven SVGs
- SVG vs Canvas comparison
