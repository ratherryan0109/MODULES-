# JavaScript Localization - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Localization - key patterns
const locale_detection = "Locale detection";
const result = locale_detection.toUpperCase();
console.log(result); // "LOCALE DETECTION"
```

### Key Concepts

- **Locale detection**: Core concept in javascript localization
- **Translation strategies**: Core concept in javascript localization
- **Intl API localization**: Core concept in javascript localization
- **Pluralization rules**: Core concept in javascript localization
- **Date/time localization**: Core concept in javascript localization
- **Number/currency formatting**: Core concept in javascript localization
- **RTL (right-to-left) support**: Core concept in javascript localization
- **i18n libraries (i18next)**: Core concept in javascript localization

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Localization"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Localization"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Locale detection
- Translation strategies
- Intl API localization
- Pluralization rules
- Date/time localization
- Number/currency formatting
- RTL (right-to-left) support
- i18n libraries (i18next)
