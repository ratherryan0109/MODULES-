[
  {
    "id": "63-challenge-1",
    "title": "Basic JavaScript Generators",
    "description": "Write a function that demonstrates the core concepts of JavaScript Generators. Focus on implementing Generator function syntax (function*) correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_63() {\n  // Implement generator function syntax (function*)\n  // Return a meaningful result\n  return \"Implement JavaScript Generators\";\n}",
    "solution": "function basic_63() {\n  const concepts = [\"Generator function syntax (function*)\", \"yield keyword\", \"Generator.next() method\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "63-challenge-2",
    "title": "Intermediate JavaScript Generators",
    "description": "Create a function that uses Generator function syntax (function*) and yield keyword together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_63(input) {\n  // Process input using generator function syntax (function*)\n  // and yield keyword\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_63(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "63-challenge-3",
    "title": "Advanced JavaScript Generators Project",
    "description": "Build a small application that applies Generator function syntax (function*), yield keyword, Generator.next() method in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptGenerators {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using generator function syntax (function*)\n  }\n}",
    "solution": "class JavaScriptGenerators {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "63-challenge-4",
    "title": "Debugging JavaScript Generators",
    "description": "The following code has bugs related to Generator function syntax (function*). Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData63(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData63([\"a\", \"b\", \"c\"]));",
    "solution": "function processData63(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData63([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "63-challenge-5",
    "title": "Refactoring JavaScript Generators",
    "description": "Refactor the following code to use modern JavaScript patterns with Generator function syntax (function*) and yield keyword.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay63(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored63(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]