# JavaScript GraphQL Basics - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript GraphQL Basics - key patterns
const graphql_vs_rest = "GraphQL vs REST";
const result = graphql_vs_rest.toUpperCase();
console.log(result); // "GRAPHQL VS REST"
```

### Key Concepts

- **GraphQL vs REST**: Core concept in javascript graphql basics
- **Queries and fields**: Core concept in javascript graphql basics
- **Mutations**: Core concept in javascript graphql basics
- **Subscriptions**: Core concept in javascript graphql basics
- **Schema definition (SDL)**: Core concept in javascript graphql basics
- **Resolvers**: Core concept in javascript graphql basics
- **Apollo Client**: Core concept in javascript graphql basics
- **GraphQL playground**: Core concept in javascript graphql basics

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript GraphQL Basics"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript GraphQL Basics"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- GraphQL vs REST
- Queries and fields
- Mutations
- Subscriptions
- Schema definition (SDL)
- Resolvers
- Apollo Client
- GraphQL playground
