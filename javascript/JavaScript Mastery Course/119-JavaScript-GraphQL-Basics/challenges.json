[
  {
    "id": "119-challenge-1",
    "title": "Basic JavaScript GraphQL Basics",
    "description": "Write a function that demonstrates the core concepts of JavaScript GraphQL Basics. Focus on implementing GraphQL vs REST correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_119() {\n  // Implement graphql vs rest\n  // Return a meaningful result\n  return \"Implement JavaScript GraphQL Basics\";\n}",
    "solution": "function basic_119() {\n  const concepts = [\"GraphQL vs REST\", \"Queries and fields\", \"Mutations\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "119-challenge-2",
    "title": "Intermediate JavaScript GraphQL Basics",
    "description": "Create a function that uses GraphQL vs REST and Queries and fields together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_119(input) {\n  // Process input using graphql vs rest\n  // and queries and fields\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_119(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "119-challenge-3",
    "title": "Advanced JavaScript GraphQL Basics Project",
    "description": "Build a small application that applies GraphQL vs REST, Queries and fields, Mutations in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptGraphQLBasics {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using graphql vs rest\n  }\n}",
    "solution": "class JavaScriptGraphQLBasics {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "119-challenge-4",
    "title": "Debugging JavaScript GraphQL Basics",
    "description": "The following code has bugs related to GraphQL vs REST. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData119(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData119([\"a\", \"b\", \"c\"]));",
    "solution": "function processData119(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData119([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "119-challenge-5",
    "title": "Refactoring JavaScript GraphQL Basics",
    "description": "Refactor the following code to use modern JavaScript patterns with GraphQL vs REST and Queries and fields.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay119(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored119(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]