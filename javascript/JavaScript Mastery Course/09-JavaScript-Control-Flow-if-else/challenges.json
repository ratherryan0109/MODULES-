[
  {
    "id": "09-challenge-1",
    "title": "Basic JavaScript Control Flow: if/else",
    "description": "Write a function that demonstrates the core concepts of JavaScript Control Flow: if/else. Focus on implementing if statement correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_09() {\n  // Implement if statement\n  // Return a meaningful result\n  return \"Implement JavaScript Control Flow: if/else\";\n}",
    "solution": "function basic_09() {\n  const concepts = [\"if statement\", \"else clause\", \"else if chains\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "09-challenge-2",
    "title": "Intermediate JavaScript Control Flow: if/else",
    "description": "Create a function that uses if statement and else clause together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_09(input) {\n  // Process input using if statement\n  // and else clause\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_09(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "09-challenge-3",
    "title": "Advanced JavaScript Control Flow: if/else Project",
    "description": "Build a small application that applies if statement, else clause, else if chains in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptControlFlowifelse {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using if statement\n  }\n}",
    "solution": "class JavaScriptControlFlowifelse {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "09-challenge-4",
    "title": "Debugging JavaScript Control Flow: if/else",
    "description": "The following code has bugs related to if statement. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData09(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData09([\"a\", \"b\", \"c\"]));",
    "solution": "function processData09(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData09([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "09-challenge-5",
    "title": "Refactoring JavaScript Control Flow: if/else",
    "description": "Refactor the following code to use modern JavaScript patterns with if statement and else clause.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay09(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored09(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]