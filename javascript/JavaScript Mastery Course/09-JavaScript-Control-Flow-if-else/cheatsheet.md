# JavaScript Control Flow: if/else - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Control Flow: if/else - key patterns
const if_statement = "if statement";
const result = if_statement.toUpperCase();
console.log(result); // "IF STATEMENT"
```

### Key Concepts

- **if statement**: Core concept in javascript control flow: if/else
- **else clause**: Core concept in javascript control flow: if/else
- **else if chains**: Core concept in javascript control flow: if/else
- **Nested conditionals**: Core concept in javascript control flow: if/else
- **Conditional expression patterns**: Core concept in javascript control flow: if/else
- **Truthy/falsy in conditions**: Core concept in javascript control flow: if/else
- **Early return pattern**: Core concept in javascript control flow: if/else
- **Guard clauses**: Core concept in javascript control flow: if/else

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Control Flow: if/else"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Control Flow: if/else"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- if statement
- else clause
- else if chains
- Nested conditionals
- Conditional expression patterns
- Truthy/falsy in conditions
- Early return pattern
- Guard clauses
