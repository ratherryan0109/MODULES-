# JavaScript Constructor Functions - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Constructor Functions - key patterns
const constructor_function_pattern = "Constructor function pattern";
const result = constructor_function_pattern.toUpperCase();
console.log(result); // "CONSTRUCTOR FUNCTION PATTERN"
```

### Key Concepts

- **Constructor function pattern**: Core concept in javascript constructor functions
- **new keyword operation**: Core concept in javascript constructor functions
- **Instance properties vs prototype methods**: Core concept in javascript constructor functions
- **Constructor property**: Core concept in javascript constructor functions
- **instanceof operator**: Core concept in javascript constructor functions
- **Returning objects from constructors**: Core concept in javascript constructor functions
- **Constructor vs regular function**: Core concept in javascript constructor functions
- **Built-in constructors**: Core concept in javascript constructor functions

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Constructor Functions"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Constructor Functions"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Constructor function pattern
- new keyword operation
- Instance properties vs prototype methods
- Constructor property
- instanceof operator
- Returning objects from constructors
- Constructor vs regular function
- Built-in constructors
