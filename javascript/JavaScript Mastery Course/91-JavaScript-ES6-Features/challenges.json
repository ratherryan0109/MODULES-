[
  {
    "id": "91-challenge-1",
    "title": "Basic JavaScript ES6 Features",
    "description": "Write a function that demonstrates the core concepts of JavaScript ES6 Features. Focus on implementing let and const correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_91() {\n  // Implement let and const\n  // Return a meaningful result\n  return \"Implement JavaScript ES6 Features\";\n}",
    "solution": "function basic_91() {\n  const concepts = [\"let and const\", \"Arrow functions\", \"Classes\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "91-challenge-2",
    "title": "Intermediate JavaScript ES6 Features",
    "description": "Create a function that uses let and const and Arrow functions together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_91(input) {\n  // Process input using let and const\n  // and arrow functions\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_91(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "91-challenge-3",
    "title": "Advanced JavaScript ES6 Features Project",
    "description": "Build a small application that applies let and const, Arrow functions, Classes in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptES6Features {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using let and const\n  }\n}",
    "solution": "class JavaScriptES6Features {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "91-challenge-4",
    "title": "Debugging JavaScript ES6 Features",
    "description": "The following code has bugs related to let and const. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData91(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData91([\"a\", \"b\", \"c\"]));",
    "solution": "function processData91(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData91([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "91-challenge-5",
    "title": "Refactoring JavaScript ES6 Features",
    "description": "Refactor the following code to use modern JavaScript patterns with let and const and Arrow functions.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay91(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored91(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]