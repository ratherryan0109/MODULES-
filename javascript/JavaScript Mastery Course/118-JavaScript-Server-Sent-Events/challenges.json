[
  {
    "id": "118-challenge-1",
    "title": "Basic JavaScript Server-Sent Events",
    "description": "Write a function that demonstrates the core concepts of JavaScript Server-Sent Events. Focus on implementing EventSource API correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_118() {\n  // Implement eventsource api\n  // Return a meaningful result\n  return \"Implement JavaScript Server-Sent Events\";\n}",
    "solution": "function basic_118() {\n  const concepts = [\"EventSource API\", \"Server-sent event format\", \"Automatic reconnection\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "118-challenge-2",
    "title": "Intermediate JavaScript Server-Sent Events",
    "description": "Create a function that uses EventSource API and Server-sent event format together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_118(input) {\n  // Process input using eventsource api\n  // and server-sent event format\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_118(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "118-challenge-3",
    "title": "Advanced JavaScript Server-Sent Events Project",
    "description": "Build a small application that applies EventSource API, Server-sent event format, Automatic reconnection in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptServerSentEvents {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using eventsource api\n  }\n}",
    "solution": "class JavaScriptServerSentEvents {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "118-challenge-4",
    "title": "Debugging JavaScript Server-Sent Events",
    "description": "The following code has bugs related to EventSource API. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData118(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData118([\"a\", \"b\", \"c\"]));",
    "solution": "function processData118(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData118([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "118-challenge-5",
    "title": "Refactoring JavaScript Server-Sent Events",
    "description": "Refactor the following code to use modern JavaScript patterns with EventSource API and Server-sent event format.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay118(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored118(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]