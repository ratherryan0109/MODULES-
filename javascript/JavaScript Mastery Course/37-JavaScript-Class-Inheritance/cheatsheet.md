# JavaScript Class Inheritance - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Class Inheritance - key patterns
const extends_keyword = "extends keyword";
const result = extends_keyword.toUpperCase();
console.log(result); // "EXTENDS KEYWORD"
```

### Key Concepts

- **extends keyword**: Core concept in javascript class inheritance
- **super() constructor call**: Core concept in javascript class inheritance
- **Method overriding**: Core concept in javascript class inheritance
- **Super method calls**: Core concept in javascript class inheritance
- **Mixin pattern**: Core concept in javascript class inheritance
- **Composition vs inheritance**: Core concept in javascript class inheritance
- **Abstract class pattern**: Core concept in javascript class inheritance
- **Instanceof with inheritance**: Core concept in javascript class inheritance

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Class Inheritance"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Class Inheritance"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- extends keyword
- super() constructor call
- Method overriding
- Super method calls
- Mixin pattern
- Composition vs inheritance
- Abstract class pattern
- Instanceof with inheritance
