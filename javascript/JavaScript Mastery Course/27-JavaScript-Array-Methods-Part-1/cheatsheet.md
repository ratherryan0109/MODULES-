# JavaScript Array Methods Part 1 - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Array Methods Part 1 - key patterns
const push_and_pop__stack_operations_ = "push and pop (stack operations)";
const result = push_and_pop__stack_operations_.toUpperCase();
console.log(result); // "PUSH AND POP (STACK OPERATIONS)"
```

### Key Concepts

- **push and pop (stack operations)**: Core concept in javascript array methods part 1
- **shift and unshift (queue operations)**: Core concept in javascript array methods part 1
- **splice (add/remove/replace)**: Core concept in javascript array methods part 1
- **slice (extract subarray)**: Core concept in javascript array methods part 1
- **concat (merge arrays)**: Core concept in javascript array methods part 1
- **join (array to string)**: Core concept in javascript array methods part 1
- **indexOf and includes**: Core concept in javascript array methods part 1
- **fill and copyWithin**: Core concept in javascript array methods part 1

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Array Methods Part 1"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Array Methods Part 1"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- push and pop (stack operations)
- shift and unshift (queue operations)
- splice (add/remove/replace)
- slice (extract subarray)
- concat (merge arrays)
- join (array to string)
- indexOf and includes
- fill and copyWithin
