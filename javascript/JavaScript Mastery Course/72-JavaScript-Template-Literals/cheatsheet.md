# JavaScript Template Literals - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Template Literals - key patterns
const template_literal_syntax = "Template literal syntax";
const result = template_literal_syntax.toUpperCase();
console.log(result); // "TEMPLATE LITERAL SYNTAX"
```

### Key Concepts

- **Template literal syntax**: Core concept in javascript template literals
- **Expression interpolation**: Core concept in javascript template literals
- **Multi-line strings (no \n)**: Core concept in javascript template literals
- **Tagged templates**: Core concept in javascript template literals
- **Tag function arguments**: Core concept in javascript template literals
- **String formatting with tags**: Core concept in javascript template literals
- **Custom template engines**: Core concept in javascript template literals
- **Nested templates**: Core concept in javascript template literals

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Template Literals"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Template Literals"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Template literal syntax
- Expression interpolation
- Multi-line strings (no \n)
- Tagged templates
- Tag function arguments
- String formatting with tags
- Custom template engines
- Nested templates
