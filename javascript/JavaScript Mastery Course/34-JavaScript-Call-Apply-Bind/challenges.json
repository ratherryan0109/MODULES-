[
  {
    "id": "34-challenge-1",
    "title": "Basic JavaScript call, apply, bind",
    "description": "Write a function that demonstrates the core concepts of JavaScript call, apply, bind. Focus on implementing call() - function borrowing correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_34() {\n  // Implement call() - function borrowing\n  // Return a meaningful result\n  return \"Implement JavaScript call, apply, bind\";\n}",
    "solution": "function basic_34() {\n  const concepts = [\"call() - function borrowing\", \"apply() - array arguments\", \"bind() - permanent binding\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "34-challenge-2",
    "title": "Intermediate JavaScript call, apply, bind",
    "description": "Create a function that uses call() - function borrowing and apply() - array arguments together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_34(input) {\n  // Process input using call() - function borrowing\n  // and apply() - array arguments\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_34(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "34-challenge-3",
    "title": "Advanced JavaScript call, apply, bind Project",
    "description": "Build a small application that applies call() - function borrowing, apply() - array arguments, bind() - permanent binding in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptCallApplyBind {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using call() - function borrowing\n  }\n}",
    "solution": "class JavaScriptCallApplyBind {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "34-challenge-4",
    "title": "Debugging JavaScript call, apply, bind",
    "description": "The following code has bugs related to call() - function borrowing. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData34(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData34([\"a\", \"b\", \"c\"]));",
    "solution": "function processData34(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData34([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "34-challenge-5",
    "title": "Refactoring JavaScript call, apply, bind",
    "description": "Refactor the following code to use modern JavaScript patterns with call() - function borrowing and apply() - array arguments.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay34(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored34(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]