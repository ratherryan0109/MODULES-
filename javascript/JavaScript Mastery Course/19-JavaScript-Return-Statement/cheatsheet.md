# JavaScript Return Statement - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Return Statement - key patterns
const return_statement_syntax = "Return statement syntax";
const result = return_statement_syntax.toUpperCase();
console.log(result); // "RETURN STATEMENT SYNTAX"
```

### Key Concepts

- **Return statement syntax**: Core concept in javascript return statement
- **Implicit undefined return**: Core concept in javascript return statement
- **Early return pattern**: Core concept in javascript return statement
- **Returning multiple values**: Core concept in javascript return statement
- **Returning functions (closures)**: Core concept in javascript return statement
- **Return objects and arrays**: Core concept in javascript return statement
- **Return with ternary**: Core concept in javascript return statement
- **No return pattern**: Core concept in javascript return statement

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Return Statement"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Return Statement"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Return statement syntax
- Implicit undefined return
- Early return pattern
- Returning multiple values
- Returning functions (closures)
- Return objects and arrays
- Return with ternary
- No return pattern
