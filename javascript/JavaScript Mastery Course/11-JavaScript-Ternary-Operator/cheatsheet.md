# JavaScript Ternary Operator - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Ternary Operator - key patterns
const ternary_syntax__condition___expr1___expr2_ = "Ternary syntax (condition ? expr1 : expr2)";
const result = ternary_syntax__condition___expr1___expr2_.toUpperCase();
console.log(result); // "TERNARY SYNTAX (CONDITION ? EXPR1 : EXPR2)"
```

### Key Concepts

- **Ternary syntax (condition ? expr1 : expr2)**: Core concept in javascript ternary operator
- **Chaining ternaries**: Core concept in javascript ternary operator
- **Nested ternaries**: Core concept in javascript ternary operator
- **Readability vs conciseness**: Core concept in javascript ternary operator
- **Ternary with template literals**: Core concept in javascript ternary operator
- **Truthy/falsy in conditions**: Core concept in javascript ternary operator
- **Returning values with ternary**: Core concept in javascript ternary operator
- **Short-circuit alternatives**: Core concept in javascript ternary operator

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Ternary Operator"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Ternary Operator"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Ternary syntax (condition ? expr1 : expr2)
- Chaining ternaries
- Nested ternaries
- Readability vs conciseness
- Ternary with template literals
- Truthy/falsy in conditions
- Returning values with ternary
- Short-circuit alternatives
