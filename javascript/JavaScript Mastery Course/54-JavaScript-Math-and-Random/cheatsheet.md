# JavaScript Math and Random - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Math and Random - key patterns
const math_properties__pi__e__etc__ = "Math properties (PI, E, etc.)";
const result = math_properties__pi__e__etc__.toUpperCase();
console.log(result); // "MATH PROPERTIES (PI, E, ETC.)"
```

### Key Concepts

- **Math properties (PI, E, etc.)**: Core concept in javascript math and random
- **Math methods (abs, pow, sqrt, sin, cos)**: Core concept in javascript math and random
- **Generating random numbers**: Core concept in javascript math and random
- **Random integer in range**: Core concept in javascript math and random
- **Random element from array**: Core concept in javascript math and random
- **Cryptographic randomness**: Core concept in javascript math and random
- **Shuffling arrays**: Core concept in javascript math and random
- **Mathematical algorithms**: Core concept in javascript math and random

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Math and Random"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Math and Random"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Math properties (PI, E, etc.)
- Math methods (abs, pow, sqrt, sin, cos)
- Generating random numbers
- Random integer in range
- Random element from array
- Cryptographic randomness
- Shuffling arrays
- Mathematical algorithms
