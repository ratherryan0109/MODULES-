[
  {
    "id": "139-challenge-1",
    "title": "Basic JavaScript React Advanced",
    "description": "Write a function that demonstrates the core concepts of JavaScript React Advanced. Focus on implementing Compound components correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_139() {\n  // Implement compound components\n  // Return a meaningful result\n  return \"Implement JavaScript React Advanced\";\n}",
    "solution": "function basic_139() {\n  const concepts = [\"Compound components\", \"Render props pattern\", \"Higher-order components\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "139-challenge-2",
    "title": "Intermediate JavaScript React Advanced",
    "description": "Create a function that uses Compound components and Render props pattern together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_139(input) {\n  // Process input using compound components\n  // and render props pattern\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_139(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "139-challenge-3",
    "title": "Advanced JavaScript React Advanced Project",
    "description": "Build a small application that applies Compound components, Render props pattern, Higher-order components in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptReactAdvanced {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using compound components\n  }\n}",
    "solution": "class JavaScriptReactAdvanced {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "139-challenge-4",
    "title": "Debugging JavaScript React Advanced",
    "description": "The following code has bugs related to Compound components. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData139(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData139([\"a\", \"b\", \"c\"]));",
    "solution": "function processData139(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData139([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "139-challenge-5",
    "title": "Refactoring JavaScript React Advanced",
    "description": "Refactor the following code to use modern JavaScript patterns with Compound components and Render props pattern.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay139(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored139(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]