# JavaScript Date and Time - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Date and Time - key patterns
const date_object_creation = "Date object creation";
const result = date_object_creation.toUpperCase();
console.log(result); // "DATE OBJECT CREATION"
```

### Key Concepts

- **Date object creation**: Core concept in javascript date and time
- **get/set date methods**: Core concept in javascript date and time
- **Date formatting (toLocaleDateString)**: Core concept in javascript date and time
- **Unix timestamps**: Core concept in javascript date and time
- **Date arithmetic**: Core concept in javascript date and time
- **Timezone handling**: Core concept in javascript date and time
- **Intl.DateTimeFormat**: Core concept in javascript date and time
- **date-fns library basics**: Core concept in javascript date and time

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Date and Time"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Date and Time"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Date object creation
- get/set date methods
- Date formatting (toLocaleDateString)
- Unix timestamps
- Date arithmetic
- Timezone handling
- Intl.DateTimeFormat
- date-fns library basics
