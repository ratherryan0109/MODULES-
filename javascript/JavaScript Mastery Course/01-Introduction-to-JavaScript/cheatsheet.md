# Introduction to JavaScript - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// Introduction to JavaScript - key patterns
const javascript_history_and_evolution = "JavaScript history and evolution";
const result = javascript_history_and_evolution.toUpperCase();
console.log(result); // "JAVASCRIPT HISTORY AND EVOLUTION"
```

### Key Concepts

- **JavaScript history and evolution**: Core concept in introduction to javascript
- **Client-side vs server-side JS**: Core concept in introduction to javascript
- **Adding JS to HTML (inline, internal, external)**: Core concept in introduction to javascript
- **Browser DevTools console**: Core concept in introduction to javascript
- **JavaScript syntax basics**: Core concept in introduction to javascript
- **Comments and statements**: Core concept in introduction to javascript
- **Case sensitivity**: Core concept in introduction to javascript
- **ECMAScript versions**: Core concept in introduction to javascript

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "Introduction to JavaScript"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "Introduction to JavaScript"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- JavaScript history and evolution
- Client-side vs server-side JS
- Adding JS to HTML (inline, internal, external)
- Browser DevTools console
- JavaScript syntax basics
- Comments and statements
- Case sensitivity
- ECMAScript versions
