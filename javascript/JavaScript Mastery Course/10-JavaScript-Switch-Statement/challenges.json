[
  {
    "id": "10-challenge-1",
    "title": "Basic JavaScript Switch Statement",
    "description": "Write a function that demonstrates the core concepts of JavaScript Switch Statement. Focus on implementing switch syntax correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_10() {\n  // Implement switch syntax\n  // Return a meaningful result\n  return \"Implement JavaScript Switch Statement\";\n}",
    "solution": "function basic_10() {\n  const concepts = [\"switch syntax\", \"case clauses\", \"break statement\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "10-challenge-2",
    "title": "Intermediate JavaScript Switch Statement",
    "description": "Create a function that uses switch syntax and case clauses together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_10(input) {\n  // Process input using switch syntax\n  // and case clauses\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_10(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "10-challenge-3",
    "title": "Advanced JavaScript Switch Statement Project",
    "description": "Build a small application that applies switch syntax, case clauses, break statement in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptSwitchStatement {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using switch syntax\n  }\n}",
    "solution": "class JavaScriptSwitchStatement {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "10-challenge-4",
    "title": "Debugging JavaScript Switch Statement",
    "description": "The following code has bugs related to switch syntax. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData10(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData10([\"a\", \"b\", \"c\"]));",
    "solution": "function processData10(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData10([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "10-challenge-5",
    "title": "Refactoring JavaScript Switch Statement",
    "description": "Refactor the following code to use modern JavaScript patterns with switch syntax and case clauses.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay10(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored10(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]