# JavaScript Short-Circuiting and Nullish Coalescing - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Short-Circuiting and Nullish Coalescing - key patterns
const ___short_circuiting = "&& short-circuiting";
const result = ___short_circuiting.toUpperCase();
console.log(result); // "&& SHORT-CIRCUITING"
```

### Key Concepts

- **&& short-circuiting**: Core concept in javascript short-circuiting and nullish coalescing
- **|| short-circuiting**: Core concept in javascript short-circuiting and nullish coalescing
- **Short-circuit evaluation patterns**: Core concept in javascript short-circuiting and nullish coalescing
- **Nullish coalescing (??)**: Core concept in javascript short-circuiting and nullish coalescing
- **Optional chaining (?.)**: Core concept in javascript short-circuiting and nullish coalescing
- **Logical assignment (&&=, ||=, ??=)**: Core concept in javascript short-circuiting and nullish coalescing
- **Nullish vs falsy distinction**: Core concept in javascript short-circuiting and nullish coalescing
- **Chaining nullish operators**: Core concept in javascript short-circuiting and nullish coalescing

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Short-Circuiting and Nullish Coalescing"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Short-Circuiting and Nullish Coalescing"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- && short-circuiting
- || short-circuiting
- Short-circuit evaluation patterns
- Nullish coalescing (??)
- Optional chaining (?.)
- Logical assignment (&&=, ||=, ??=)
- Nullish vs falsy distinction
- Chaining nullish operators
