[
  {
    "id": "136-challenge-1",
    "title": "Basic JavaScript TypeScript Advanced",
    "description": "Write a function that demonstrates the core concepts of JavaScript TypeScript Advanced. Focus on implementing Conditional types correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_136() {\n  // Implement conditional types\n  // Return a meaningful result\n  return \"Implement JavaScript TypeScript Advanced\";\n}",
    "solution": "function basic_136() {\n  const concepts = [\"Conditional types\", \"Mapped types\", \"Template literal types\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "136-challenge-2",
    "title": "Intermediate JavaScript TypeScript Advanced",
    "description": "Create a function that uses Conditional types and Mapped types together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_136(input) {\n  // Process input using conditional types\n  // and mapped types\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_136(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "136-challenge-3",
    "title": "Advanced JavaScript TypeScript Advanced Project",
    "description": "Build a small application that applies Conditional types, Mapped types, Template literal types in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptTypeScriptAdvanced {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using conditional types\n  }\n}",
    "solution": "class JavaScriptTypeScriptAdvanced {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "136-challenge-4",
    "title": "Debugging JavaScript TypeScript Advanced",
    "description": "The following code has bugs related to Conditional types. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData136(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData136([\"a\", \"b\", \"c\"]));",
    "solution": "function processData136(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData136([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "136-challenge-5",
    "title": "Refactoring JavaScript TypeScript Advanced",
    "description": "Refactor the following code to use modern JavaScript patterns with Conditional types and Mapped types.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay136(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored136(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]