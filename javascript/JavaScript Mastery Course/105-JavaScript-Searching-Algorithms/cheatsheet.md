# JavaScript Searching Algorithms - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Searching Algorithms - key patterns
const linear_search = "Linear search";
const result = linear_search.toUpperCase();
console.log(result); // "LINEAR SEARCH"
```

### Key Concepts

- **Linear search**: Core concept in javascript searching algorithms
- **Binary search**: Core concept in javascript searching algorithms
- **Depth-first search (DFS)**: Core concept in javascript searching algorithms
- **Breadth-first search (BFS)**: Core concept in javascript searching algorithms
- **Binary search tree search**: Core concept in javascript searching algorithms
- **Graph traversal**: Core concept in javascript searching algorithms
- **String search (KMP algorithm)**: Core concept in javascript searching algorithms
- **Rabin-Karp algorithm**: Core concept in javascript searching algorithms

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Searching Algorithms"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Searching Algorithms"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Linear search
- Binary search
- Depth-first search (DFS)
- Breadth-first search (BFS)
- Binary search tree search
- Graph traversal
- String search (KMP algorithm)
- Rabin-Karp algorithm
