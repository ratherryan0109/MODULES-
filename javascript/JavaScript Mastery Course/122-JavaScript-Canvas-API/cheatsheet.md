# JavaScript Canvas API - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Canvas API - key patterns
const canvas_context__2d_ = "Canvas context (2D)";
const result = canvas_context__2d_.toUpperCase();
console.log(result); // "CANVAS CONTEXT (2D)"
```

### Key Concepts

- **Canvas context (2D)**: Core concept in javascript canvas api
- **Drawing shapes and paths**: Core concept in javascript canvas api
- **Colors and gradients**: Core concept in javascript canvas api
- **Text rendering**: Core concept in javascript canvas api
- **Image drawing and manipulation**: Core concept in javascript canvas api
- **Animation loop**: Core concept in javascript canvas api
- **Pixel manipulation**: Core concept in javascript canvas api
- **Canvas performance**: Core concept in javascript canvas api

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Canvas API"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Canvas API"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Canvas context (2D)
- Drawing shapes and paths
- Colors and gradients
- Text rendering
- Image drawing and manipulation
- Animation loop
- Pixel manipulation
- Canvas performance
