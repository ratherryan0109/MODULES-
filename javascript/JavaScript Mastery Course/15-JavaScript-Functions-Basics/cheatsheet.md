# JavaScript Functions Basics - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Functions Basics - key patterns
const function_declarations = "Function declarations";
const result = function_declarations.toUpperCase();
console.log(result); // "FUNCTION DECLARATIONS"
```

### Key Concepts

- **Function declarations**: Core concept in javascript functions basics
- **Function expressions**: Core concept in javascript functions basics
- **Parameters and arguments**: Core concept in javascript functions basics
- **Return values**: Core concept in javascript functions basics
- **Default parameters**: Core concept in javascript functions basics
- **Rest parameters**: Core concept in javascript functions basics
- **Function hoisting**: Core concept in javascript functions basics
- **First-class functions**: Core concept in javascript functions basics

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Functions Basics"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Functions Basics"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Function declarations
- Function expressions
- Parameters and arguments
- Return values
- Default parameters
- Rest parameters
- Function hoisting
- First-class functions
