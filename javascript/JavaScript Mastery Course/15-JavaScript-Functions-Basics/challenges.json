[
  {
    "id": "15-challenge-1",
    "title": "Basic JavaScript Functions Basics",
    "description": "Write a function that demonstrates the core concepts of JavaScript Functions Basics. Focus on implementing Function declarations correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_15() {\n  // Implement function declarations\n  // Return a meaningful result\n  return \"Implement JavaScript Functions Basics\";\n}",
    "solution": "function basic_15() {\n  const concepts = [\"Function declarations\", \"Function expressions\", \"Parameters and arguments\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "15-challenge-2",
    "title": "Intermediate JavaScript Functions Basics",
    "description": "Create a function that uses Function declarations and Function expressions together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_15(input) {\n  // Process input using function declarations\n  // and function expressions\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_15(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "15-challenge-3",
    "title": "Advanced JavaScript Functions Basics Project",
    "description": "Build a small application that applies Function declarations, Function expressions, Parameters and arguments in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptFunctionsBasics {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using function declarations\n  }\n}",
    "solution": "class JavaScriptFunctionsBasics {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "15-challenge-4",
    "title": "Debugging JavaScript Functions Basics",
    "description": "The following code has bugs related to Function declarations. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData15(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData15([\"a\", \"b\", \"c\"]));",
    "solution": "function processData15(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData15([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "15-challenge-5",
    "title": "Refactoring JavaScript Functions Basics",
    "description": "Refactor the following code to use modern JavaScript patterns with Function declarations and Function expressions.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay15(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored15(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]