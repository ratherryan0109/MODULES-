# JavaScript Sorting Algorithms - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Sorting Algorithms - key patterns
const merge_sort__divide_and_conquer_ = "Merge sort (divide and conquer)";
const result = merge_sort__divide_and_conquer_.toUpperCase();
console.log(result); // "MERGE SORT (DIVIDE AND CONQUER)"
```

### Key Concepts

- **Merge sort (divide and conquer)**: Core concept in javascript sorting algorithms
- **Quicksort (pivot partitioning)**: Core concept in javascript sorting algorithms
- **Heapsort (heap data structure)**: Core concept in javascript sorting algorithms
- **Counting sort (non-comparison)**: Core concept in javascript sorting algorithms
- **Radix sort**: Core concept in javascript sorting algorithms
- **Sorting stability**: Core concept in javascript sorting algorithms
- **Comparative performance**: Core concept in javascript sorting algorithms
- **JavaScript native sort**: Core concept in javascript sorting algorithms

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Sorting Algorithms"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Sorting Algorithms"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Merge sort (divide and conquer)
- Quicksort (pivot partitioning)
- Heapsort (heap data structure)
- Counting sort (non-comparison)
- Radix sort
- Sorting stability
- Comparative performance
- JavaScript native sort
