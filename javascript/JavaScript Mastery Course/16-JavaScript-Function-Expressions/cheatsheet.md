# JavaScript Function Expressions - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Function Expressions - key patterns
const anonymous_functions = "Anonymous functions";
const result = anonymous_functions.toUpperCase();
console.log(result); // "ANONYMOUS FUNCTIONS"
```

### Key Concepts

- **Anonymous functions**: Core concept in javascript function expressions
- **Named function expressions**: Core concept in javascript function expressions
- **Function expressions vs declarations**: Core concept in javascript function expressions
- **Hoisting differences**: Core concept in javascript function expressions
- **Callback functions**: Core concept in javascript function expressions
- **Immediately Invoked Function Expressions**: Core concept in javascript function expressions
- **Functions as values**: Core concept in javascript function expressions
- **Function expression patterns**: Core concept in javascript function expressions

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Function Expressions"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Function Expressions"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Anonymous functions
- Named function expressions
- Function expressions vs declarations
- Hoisting differences
- Callback functions
- Immediately Invoked Function Expressions
- Functions as values
- Function expression patterns
