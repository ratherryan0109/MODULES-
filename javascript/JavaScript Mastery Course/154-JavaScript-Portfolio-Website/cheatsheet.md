# JavaScript Portfolio Website - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Portfolio Website - key patterns
const portfolio_layout = "Portfolio layout";
const result = portfolio_layout.toUpperCase();
console.log(result); // "PORTFOLIO LAYOUT"
```

### Key Concepts

- **Portfolio layout**: Core concept in javascript portfolio website
- **Project showcase with filters**: Core concept in javascript portfolio website
- **Smooth scrolling animations**: Core concept in javascript portfolio website
- **Contact form with validation**: Core concept in javascript portfolio website
- **Responsive navigation**: Core concept in javascript portfolio website
- **Dark/light theme**: Core concept in javascript portfolio website
- **Performance optimization**: Core concept in javascript portfolio website
- **SEO basics**: Core concept in javascript portfolio website

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Portfolio Website"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Portfolio Website"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Portfolio layout
- Project showcase with filters
- Smooth scrolling animations
- Contact form with validation
- Responsive navigation
- Dark/light theme
- Performance optimization
- SEO basics
