[
  {
    "id": "29-challenge-1",
    "title": "Basic JavaScript Array Sorting",
    "description": "Write a function that demonstrates the core concepts of JavaScript Array Sorting. Focus on implementing sort() method correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_29() {\n  // Implement sort() method\n  // Return a meaningful result\n  return \"Implement JavaScript Array Sorting\";\n}",
    "solution": "function basic_29() {\n  const concepts = [\"sort() method\", \"Custom compare functions\", \"String sorting with localeCompare\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "29-challenge-2",
    "title": "Intermediate JavaScript Array Sorting",
    "description": "Create a function that uses sort() method and Custom compare functions together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_29(input) {\n  // Process input using sort() method\n  // and custom compare functions\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_29(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "29-challenge-3",
    "title": "Advanced JavaScript Array Sorting Project",
    "description": "Build a small application that applies sort() method, Custom compare functions, String sorting with localeCompare in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptArraySorting {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using sort() method\n  }\n}",
    "solution": "class JavaScriptArraySorting {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "29-challenge-4",
    "title": "Debugging JavaScript Array Sorting",
    "description": "The following code has bugs related to sort() method. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData29(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData29([\"a\", \"b\", \"c\"]));",
    "solution": "function processData29(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData29([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "29-challenge-5",
    "title": "Refactoring JavaScript Array Sorting",
    "description": "Refactor the following code to use modern JavaScript patterns with sort() method and Custom compare functions.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay29(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored29(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]