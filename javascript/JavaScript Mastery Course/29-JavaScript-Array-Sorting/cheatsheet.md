# JavaScript Array Sorting - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Array Sorting - key patterns
const sort___method = "sort() method";
const result = sort___method.toUpperCase();
console.log(result); // "SORT() METHOD"
```

### Key Concepts

- **sort() method**: Core concept in javascript array sorting
- **Custom compare functions**: Core concept in javascript array sorting
- **String sorting with localeCompare**: Core concept in javascript array sorting
- **Sorting numbers correctly**: Core concept in javascript array sorting
- **Sorting objects by property**: Core concept in javascript array sorting
- **Stable sort (ES2019)**: Core concept in javascript array sorting
- **Reverse sorting**: Core concept in javascript array sorting
- **Sorting performance**: Core concept in javascript array sorting

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Array Sorting"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Array Sorting"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- sort() method
- Custom compare functions
- String sorting with localeCompare
- Sorting numbers correctly
- Sorting objects by property
- Stable sort (ES2019)
- Reverse sorting
- Sorting performance
