# JavaScript Animation Basics - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Animation Basics - key patterns
const requestanimationframe_loop = "requestAnimationFrame loop";
const result = requestanimationframe_loop.toUpperCase();
console.log(result); // "REQUESTANIMATIONFRAME LOOP"
```

### Key Concepts

- **requestAnimationFrame loop**: Core concept in javascript animation basics
- **Easing functions**: Core concept in javascript animation basics
- **CSS animations via JavaScript**: Core concept in javascript animation basics
- **Web Animations API**: Core concept in javascript animation basics
- **Timeline-based animation**: Core concept in javascript animation basics
- **Scroll-based animation**: Core concept in javascript animation basics
- **Animation performance**: Core concept in javascript animation basics
- **Hardware-accelerated animation**: Core concept in javascript animation basics

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Animation Basics"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Animation Basics"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- requestAnimationFrame loop
- Easing functions
- CSS animations via JavaScript
- Web Animations API
- Timeline-based animation
- Scroll-based animation
- Animation performance
- Hardware-accelerated animation
