[
  {
    "id": "95-challenge-1",
    "title": "Basic JavaScript ES13/ES14 Features",
    "description": "Write a function that demonstrates the core concepts of JavaScript ES13/ES14 Features. Focus on implementing Class fields (public, private #) correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_95() {\n  // Implement class fields (public, private #)\n  // Return a meaningful result\n  return \"Implement JavaScript ES13/ES14 Features\";\n}",
    "solution": "function basic_95() {\n  const concepts = [\"Class fields (public, private #)\", \"Private methods (#method)\", \"Static initialization blocks\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "95-challenge-2",
    "title": "Intermediate JavaScript ES13/ES14 Features",
    "description": "Create a function that uses Class fields (public, private #) and Private methods (#method) together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_95(input) {\n  // Process input using class fields (public, private #)\n  // and private methods (#method)\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_95(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "95-challenge-3",
    "title": "Advanced JavaScript ES13/ES14 Features Project",
    "description": "Build a small application that applies Class fields (public, private #), Private methods (#method), Static initialization blocks in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptES13ES14Features {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using class fields (public, private #)\n  }\n}",
    "solution": "class JavaScriptES13ES14Features {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "95-challenge-4",
    "title": "Debugging JavaScript ES13/ES14 Features",
    "description": "The following code has bugs related to Class fields (public, private #). Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData95(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData95([\"a\", \"b\", \"c\"]));",
    "solution": "function processData95(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData95([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "95-challenge-5",
    "title": "Refactoring JavaScript ES13/ES14 Features",
    "description": "Refactor the following code to use modern JavaScript patterns with Class fields (public, private #) and Private methods (#method).",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay95(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored95(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]