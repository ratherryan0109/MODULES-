# JavaScript Parameters and Arguments - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Parameters and Arguments - key patterns
const default_parameters = "Default parameters";
const result = default_parameters.toUpperCase();
console.log(result); // "DEFAULT PARAMETERS"
```

### Key Concepts

- **Default parameters**: Core concept in javascript parameters and arguments
- **Rest parameters (...args)**: Core concept in javascript parameters and arguments
- **arguments object**: Core concept in javascript parameters and arguments
- **Destructuring parameters**: Core concept in javascript parameters and arguments
- **Parameter passing (by value)**: Core concept in javascript parameters and arguments
- **Parameter validation**: Core concept in javascript parameters and arguments
- **Named parameters pattern**: Core concept in javascript parameters and arguments
- **Spread operator in function calls**: Core concept in javascript parameters and arguments

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Parameters and Arguments"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Parameters and Arguments"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Default parameters
- Rest parameters (...args)
- arguments object
- Destructuring parameters
- Parameter passing (by value)
- Parameter validation
- Named parameters pattern
- Spread operator in function calls
