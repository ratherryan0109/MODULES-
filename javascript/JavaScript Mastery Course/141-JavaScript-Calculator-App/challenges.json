[
  {
    "id": "141-challenge-1",
    "title": "Basic JavaScript Calculator App",
    "description": "Write a function that demonstrates the core concepts of JavaScript Calculator App. Focus on implementing UI design for calculator correctly.",
    "difficulty": "easy",
    "starterCode": "function basic_141() {\n  // Implement ui design for calculator\n  // Return a meaningful result\n  return \"Implement JavaScript Calculator App\";\n}",
    "solution": "function basic_141() {\n  const concepts = [\"UI design for calculator\", \"Expression parsing\", \"Keyboard event handling\"];\n  return concepts.map(c => c.toUpperCase());\n}"
  },
  {
    "id": "141-challenge-2",
    "title": "Intermediate JavaScript Calculator App",
    "description": "Create a function that uses UI design for calculator and Expression parsing together. Handle edge cases and validate inputs.",
    "difficulty": "medium",
    "starterCode": "function intermediate_141(input) {\n  // Process input using ui design for calculator\n  // and expression parsing\n  // Handle edge cases\n  if (!input) return [];\n  return input;\n}",
    "solution": "function intermediate_141(input) {\n  if (!Array.isArray(input)) return [];\n  return input\n    .filter(item => item != null)\n    .map(item => ({ original: item, processed: String(item).toUpperCase() }));\n}"
  },
  {
    "id": "141-challenge-3",
    "title": "Advanced JavaScript Calculator App Project",
    "description": "Build a small application that applies UI design for calculator, Expression parsing, Keyboard event handling in a real-world scenario. Include error handling and input validation.",
    "difficulty": "hard",
    "starterCode": "class JavaScriptCalculatorApp {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    // Add item with validation\n  }\n  \n  process() {\n    // Process stored data using ui design for calculator\n  }\n}",
    "solution": "class JavaScriptCalculatorApp {\n  constructor(config) {\n    this.config = config || {};\n    this.data = [];\n  }\n  \n  add(item) {\n    if (item == null) throw new Error('Invalid item');\n    this.data.push(item);\n  }\n  \n  process() {\n    return this.data\n      .filter(d => d != null)\n      .map(d => ({ value: d, timestamp: Date.now() }));\n  }\n  \n  clear() {\n    this.data = [];\n  }\n}"
  },
  {
    "id": "141-challenge-4",
    "title": "Debugging JavaScript Calculator App",
    "description": "The following code has bugs related to UI design for calculator. Find and fix them.",
    "difficulty": "medium",
    "starterCode": "// Fix the bugs in this code\nfunction processData141(items) {\n  let result = [];\n  for (var i = 0; i < items.length; i++); {\n    result.push(items[i].toUpperCase());\n  }\n  return result;\n}\n\nconsole.log(processData141([\"a\", \"b\", \"c\"]));",
    "solution": "function processData141(items) {\n  return items.map(item => item.toUpperCase());\n}\n\nconsole.log(processData141([\"a\", \"b\", \"c\"]));\n// Expected: [\"A\", \"B\", \"C\"]"
  },
  {
    "id": "141-challenge-5",
    "title": "Refactoring JavaScript Calculator App",
    "description": "Refactor the following code to use modern JavaScript patterns with UI design for calculator and Expression parsing.",
    "difficulty": "hard",
    "starterCode": "// Refactor this code\nfunction oldWay141(arr) {\n  var temp = [];\n  for (var i = 0; i < arr.length; i++) {\n    if (arr[i] % 2 === 0) {\n      temp.push(arr[i] * 2);\n    }\n  }\n  var sum = 0;\n  for (var j = 0; j < temp.length; j++) {\n    sum += temp[j];\n  }\n  return sum;\n}",
    "solution": "function refactored141(arr) {\n  return arr\n    .filter(n => n % 2 === 0)\n    .map(n => n * 2)\n    .reduce((sum, n) => sum + n, 0);\n}"
  }
]