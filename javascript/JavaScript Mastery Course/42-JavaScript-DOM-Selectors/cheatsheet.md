# JavaScript DOM Selectors - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript DOM Selectors - key patterns
const css_selectors_with_queryselector = "CSS selectors with querySelector";
const result = css_selectors_with_queryselector.toUpperCase();
console.log(result); // "CSS SELECTORS WITH QUERYSELECTOR"
```

### Key Concepts

- **CSS selectors with querySelector**: Core concept in javascript dom selectors
- **querySelectorAll static collection**: Core concept in javascript dom selectors
- **getElementById fast lookup**: Core concept in javascript dom selectors
- **Live HTMLCollection vs static NodeList**: Core concept in javascript dom selectors
- **Closest() method**: Core concept in javascript dom selectors
- **Matches() method**: Core concept in javascript dom selectors
- **Selecting children and parents**: Core concept in javascript dom selectors
- **Selector performance**: Core concept in javascript dom selectors

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript DOM Selectors"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript DOM Selectors"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- CSS selectors with querySelector
- querySelectorAll static collection
- getElementById fast lookup
- Live HTMLCollection vs static NodeList
- Closest() method
- Matches() method
- Selecting children and parents
- Selector performance
