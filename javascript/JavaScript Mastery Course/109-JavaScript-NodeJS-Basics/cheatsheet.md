# JavaScript Node.js Basics - Cheatsheet

## Quick Reference

### Core Syntax
```javascript
// JavaScript Node.js Basics - key patterns
const node_js_architecture = "Node.js architecture";
const result = node_js_architecture.toUpperCase();
console.log(result); // "NODE.JS ARCHITECTURE"
```

### Key Concepts

- **Node.js architecture**: Core concept in javascript node.js basics
- **Core modules (fs, path, os)**: Core concept in javascript node.js basics
- **Reading and writing files**: Core concept in javascript node.js basics
- **Streams and buffers**: Core concept in javascript node.js basics
- **Events and EventEmitter**: Core concept in javascript node.js basics
- **Process and environment**: Core concept in javascript node.js basics
- **Command-line arguments**: Core concept in javascript node.js basics
- **npm and package.json**: Core concept in javascript node.js basics

### Common Operations

| Operation | Syntax | Example |
|-----------|--------|---------|
| Declaration | `const x = value` | `const topic = "JavaScript Node.js Basics"` |
| Check type | `typeof x` | `typeof topic // "string"` |
| Transform | `x.method()` | `topic.toLowerCase()` |
| Compare | `x === y` | `topic === "JavaScript Node.js Basics"` |
| Iterate | `arr.method()` | `[1,2,3].map(n => n * 2)` |

### Best Practices
1. Use `const` by default, `let` when reassignment is needed
2. Handle edge cases and validate inputs
3. Write pure functions when possible
4. Use meaningful, descriptive names
5. Keep functions small and focused
6. Test with different inputs and edge cases
7. Use early returns to reduce nesting
8. Prefer array methods over manual loops

### Common Mistakes
- Misunderstanding mutable vs immutable operations
- Forgetting to handle edge cases
- Overcomplicating simple logic
- Not using strict equality (===)
- Mutating state unexpectedly

### Related Topics
- Node.js architecture
- Core modules (fs, path, os)
- Reading and writing files
- Streams and buffers
- Events and EventEmitter
- Process and environment
- Command-line arguments
- npm and package.json
